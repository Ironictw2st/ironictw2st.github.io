// data/se_docs.js
// GENERATED by generate_data/fetch_tool_docs.py - do not edit by hand.
// Source: Z:\Claude\ScriptExtender\docs\*.md
const SE_DOCS = {
  "docs": [
    {
      "id": "scripting",
      "title": "Campaign Lua API",
      "file": "SCRIPTING.md",
      "source": "https://github.com/Ironictw2st/TK-ScriptExtender/blob/master/docs/SCRIPTING.md",
      "sections": [
        {
          "level": 1,
          "id": "script-extender-campaign-lua-api",
          "title": "Script Extender: campaign Lua API",
          "text": "Script Extender: campaign Lua API",
          "html": "<p>Scripting reference for the <code>se.*</code> API that the script extender DLL adds to Total War: THREE KINGDOMS <strong>build 1.7.2.0</strong>. Audience: mod authors who already know 3K campaign scripting (<code>cm</code>, <code>core:add_listener</code>, <code>QUERY_*</code> / <code>MODIFY_*</code> interfaces).</p><p>Sources of truth for this document: <code>crates/script_extender/lua/se_api.lua</code> (signatures), the DLL sources under <code>crates/script_extender/src/</code> (defaults, refusals), <code>HANDOFF.md</code> (injection rules, version history), <code>notes/*.md</code> (behaviour, live verification). Current DLL version at the time of writing: <strong>0.40.0</strong> (2026-09-19), which is the 0.37 beta line on the stable channel.</p>"
        },
        {
          "level": 3,
          "id": "which-dll-version-a-feature-needs",
          "title": "Which DLL version a feature needs",
          "text": "Which DLL version a feature needs",
          "html": "<p>Every function entry in §3 carries its own <code>Since</code> line; this is the overview. The mod manager compares versions as dotted integers, so <strong>0.40 &gt; 0.37 &gt; 0.4</strong>.</p><div class=\"doc-table\"><table><thead><tr><th>DLL</th><th>Brings</th></tr></thead><tbody><tr><td>0.8</td><td>the embedded <code>se_api.lua</code> module itself, characters / pools, retinues, units, progression</td></tr><tr><td>0.10 - 0.12</td><td>disband, world-leader seats, assignments, flat XP, skill points, CAI personality, faction potential</td></tr><tr><td>0.16 - 0.18</td><td>menu build number and <code>script_extender.cfg</code></td></tr><tr><td>0.19 - 0.23</td><td>buildings, alliance names, effect bundles, attitude events, script-side income lines</td></tr><tr><td>0.24 - 0.27</td><td>auto-resolve: tunables, prediction, plans (winner + casualties 0.26.2, duels 0.27)</td></tr><tr><td>0.28 - 0.30</td><td>horde income hook, <strong>multiplayer support with the enforced version lock</strong></td></tr><tr><td>0.31 - 0.35</td><td>performance work and diagnostics (<code>se.profile.*</code>, <code>se.query.perf</code>, <code>se.diag.*</code>)</td></tr><tr><td>0.36</td><td>AI recruitment trace (<code>se.ai_recruit.trace/report</code>, <code>se.query.ai_recruitment/unit_quality</code>)</td></tr><tr><td>0.37</td><td>AI recruitment policy (<code>se.ai_recruit.plan/execute/enable</code>)</td></tr><tr><td><strong>0.40</strong></td><td>the current stable release: everything above, horde income on by default</td></tr></tbody></table></div><hr>"
        },
        {
          "level": 2,
          "id": "1-overview",
          "title": "1. Overview",
          "text": "1. Overview",
          "html": ""
        },
        {
          "level": 3,
          "id": "what-the-script-extender-is",
          "title": "What the script extender is",
          "text": "What the script extender is",
          "html": "<p>A native DLL injected into the running <code>Three_Kingdoms.exe</code>. It:</p><ol><li>fingerprints the executable and verifies every engine address it uses by its first bytes; on any mismatch it logs and does nothing, so a patched/updated game is safe but inert;</li><li>detours <code>lua_gettop</code> and, the first time each Lua state passes through it, registers a set of <code>se_*</code> C natives into that state's globals and runs an embedded Lua module (<code>se_api.lua</code>), which defines the public <code>se.query.*</code> / <code>se.modify.*</code> / <code>se.autoresolve.*</code> API;</li><li>exposes only named, high-level operations. There is deliberately no generic peek/poke/call primitive.</li></ol><p>The natives (<code>se_recruit_unit</code>, <code>se_slot_construct</code>, ...) are internal. <strong>Scripts use the <code>se</code> module only.</strong></p>"
        },
        {
          "level": 3,
          "id": "multiplayer-dll-0-30",
          "title": "Multiplayer (DLL 0.30+)",
          "text": "Multiplayer (DLL 0.30+)",
          "html": "<p>Campaign multiplayer is lockstep: every machine runs the same scripts and must make the same change at the same model tick. The API follows three rules so that it can be used there:</p><ol><li><strong><code>se.modify.*</code> runs only inside a model callback in multiplayer</strong> (event listeners, first tick, turn start, dilemma choices: anything where <code>cm:can_modify()</code> is true). Called from a UI click, a timer or the in-game console it returns <code>false, \"refused in multiplayer: ... called outside a model callback ...\"</code> instead of being queued: only that machine would change its model and the game would desync. In single player such calls are still queued through <code>cm:wait_for_model_sp</code>.</li><li><strong>Nothing a synced script does may depend on which machine it runs on.</strong> Do not branch on <code>cm:get_local_faction()</code> or on <code>ctx.*.is_local_player</code> in code that ends in a modify call; use <code>is_human</code> and faction keys. The auto-resolve handler is called for every battle with a human player, on every machine, and chance-based duel rules are seeded from <code>ctx.seed</code> (turn number and force cqis), never from anything machine-specific. Do not use <code>math.random</code> or <code>os.time</code> to decide a change; use the model's own random functions.</li><li><strong>Both machines must run the same script extender with the same simulation settings.</strong> The DLL enforces this through the game's build string, which the multiplayer lobby compares: it always ends up containing the DLL version and a fingerprint of the five settings that can change the simulation — <code>autoresolve_hooks</code>, <code>ai_recruit_cache</code>, <code>recruit_perm_cache</code>, <code>horde_income</code> and <code>horde_income_category</code> (<code>script_extender.cfg</code> text may use <code>{version}</code> and <code>{sync}</code>; otherwise <code> [se &lt;version&gt;.&lt;sync&gt;]</code> is appended; without cfg text the game's own string is extended; <code>se.modify.build_number</code> cannot remove it). A player without the DLL, with another DLL version or with different hook settings shows a different build and cannot join. This relies on the lobby's version check using that string: <strong>not verified on two machines yet.</strong></li></ol><p>Session-only state (auto-resolver variables, redefined bundles, income lines, emperor policy) has to be re-applied by the mod after loading, from a callback that runs on every machine.</p><p>Saves made after using <code>se.modify.*</code> carry the results — they are ordinary engine state, and the DLL is not required to load such a save, only to keep using the API.</p>"
        },
        {
          "level": 3,
          "id": "detecting-the-api",
          "title": "Detecting the API",
          "text": "Detecting the API",
          "html": "<pre><code class=\"lang-lua\">if type(se) ~= \"table\" then\n    ModLog(\"script extender not present in this Lua state\")\n    return\nend\nModLog(\"script extender \" .. se.version())\nif not se.available(\"se_slot_construct\") then\n    ModLog(\"this DLL is too old for buildings\")\nend</code></pre><ul><li><code>se.version()</code> -&gt; the DLL version string, or <code>\"unknown (DLL older than 0.8)\"</code> when the <code>se_version</code> native is missing.</li><li><code>se.available(native_name)</code> -&gt; <code>true</code> when that native exists in this Lua state. Use it to feature-gate against older DLL builds. Native names per feature are listed with each function below.</li></ul>"
        },
        {
          "level": 3,
          "id": "setup-lines-every-script-needs",
          "title": "Setup lines every script needs",
          "text": "Setup lines every script needs",
          "html": "<p>The module chunk runs inside the DLL, and its environment does <strong>not</strong> see the script libraries' globals. Two objects must be handed over explicitly:</p><pre><code class=\"lang-lua\">se.logger = ModLog     -- any function(string); otherwise output goes to the DLL log\nse.core   = core       -- the event manager; needed by everything that installs a listener:\n                       -- emperor_policy, faction_income, autoresolver_variable,\n                       -- se.autoresolve.set_handler, se.diag.listeners_start,\n                       -- se.ai_recruit.enable</code></pre><p>Without <code>se.logger</code>, the module falls back to a global <code>ModLog</code> if one is visible, else to the DLL's own <code>se_log</code> (which writes to <code>script_extender.log</code>). Without <code>se.core</code>, the functions that install listeners return <code>false, \"core (event manager) is not available: set se.core = core ...\"</code>.</p>"
        },
        {
          "level": 3,
          "id": "how-results-are-returned",
          "title": "How results are returned",
          "text": "How results are returned",
          "html": "<ul><li><strong>Queries</strong> (<code>se.query.*</code>) return a value or table on success, and <code>nil, message</code> on failure.</li><li><strong>Modifiers</strong> (<code>se.modify.*</code>) return <code>ok, message</code>. <code>ok == true</code> means <em>done or queued</em>.</li></ul>"
        },
        {
          "level": 3,
          "id": "the-model-thread-rule",
          "title": "The model-thread rule",
          "text": "The model-thread rule",
          "html": "<p>Model mutation is only legal on the campaign model thread. Almost every <code>se.modify.*</code> call goes through <code>se.on_model</code>:</p><ul><li>when <code>cm:can_modify()</code> is already true (inside an event handler, a <code>cm:wait_for_model_sp</code> callback, ...), the work runs immediately and the <strong>real</strong> <code>ok, message</code> is returned;</li><li>otherwise the work is queued with <code>cm:wait_for_model_sp</code>, and the call returns <code>true, \"&lt;tag&gt; queued on the model thread (result in the log / callback)\"</code>. The real result is written to the log and passed to the optional callback where a function supports one.</li></ul><pre><code class=\"lang-lua\">se.on_model(\"my step\", function()\n    -- runs where cm:can_modify() is true\n    return true, \"ok\"\nend, function(ok, msg) ModLog(\"finished: \" .. tostring(ok) .. \" \" .. tostring(msg)) end)</code></pre><p><code>se.on_model(tag, f [, cb])</code> is public; use it to wrap your own follow-up reads (for example <code>se.query.cai_personality</code>, which also needs the model thread).</p><p><strong>Four modifiers do not go through it</strong>, because they change no model state: <code>build_number</code> (process-local UI strings), <code>autoresolve_plan_clear</code> (a DLL-side slot), and the two that only keep script-side state and install a listener, <code>emperor_policy</code> and <code>faction_income</code>. They return the real result directly and do not check multiplayer — but what their listeners <em>do</em> is model state, so install them from code that runs on every machine.</p>"
        },
        {
          "level": 3,
          "id": "logging",
          "title": "Logging",
          "text": "Logging",
          "html": "<p><code>se.log(s)</code> prefixes <code>[se] </code> and writes through <code>se.logger</code> / <code>ModLog</code> / <code>se_log</code>. Queued modifiers log their real result there. In the in-game console environment, <code>ModLog</code> output lands in <code>lua_mod_log.txt</code> and in the per-session <code>ironic_log_&lt;faction&gt;_&lt;stamp&gt;.txt</code>.</p><hr>"
        },
        {
          "level": 2,
          "id": "2-conventions-and-gotchas",
          "title": "2. Conventions and gotchas",
          "text": "2. Conventions and gotchas",
          "html": "<p><strong>Lua is 5.1 and this build has quirks:</strong></p><ul><li><strong><code>lua_Number</code> is a 32-bit float.</strong> Integers are exact only up to 16,777,216 (2^24). Large cqis and treasury values above that lose precision when they cross the Lua boundary. Keep an eye on it when you compute with treasury numbers; character cqis in a campaign are far below the limit in practice (~1,800 characters in the grand campaign).</li><li><strong><code>string.find</code> takes only two arguments in this build.</strong> A four-argument call (<code>string.find(s, p, 1, true)</code>) silently never matches. Same for the <code>init</code>/<code>plain</code> parameters of the method form.</li><li><code>cm:method</code> without a call is a syntax error; list interfaces have no <code>is_null_interface</code> (use <code>se.is_null(v)</code>, which pcall-guards the check).</li><li>Null interfaces answer any method with a function, so every engine call inside the module is <code>pcall</code>-guarded. Do the same in your own code.</li></ul><p><strong><code>cm:callback</code> does not fire from the in-game Lua console.</strong> Chain follow-ups with <code>cm:wait_for_model_sp</code> instead (the model queue runs in order). This matters for two API functions that schedule their own follow-up through <code>cm:callback</code>:</p><ul><li><code>se.modify.recruit</code> applies <code>opts.hp</code> / <code>opts.experience</code> one second after the recruit;</li><li><code>se.modify.move_character</code> runs its pool/recruited step one second after a faction move.</li></ul><p>From a normal mod script (<code>script/campaign/mod/...</code>) these work. From the console they do not fire, so call <code>se.modify.unit_strength</code> / <code>unit_experience</code> or the release step yourself in a <code>cm:wait_for_model_sp</code> block.</p><p><strong>The console caches a script's text</strong> until its panel is closed and reopened; after editing a script, close and reopen the console before loading it again.</p><p><strong>Finding pool characters.</strong> <code>QUERY_FACTION:character_list()</code> contains only recruited characters. Recruitment-pool characters are reachable by cqi (<code>cm:query_character(cqi)</code>), through the <code>NewCharacterEnteredRecruitmentPool</code> event, or by scanning cqis. Compare <code>q:command_queue_index()</code> with <code>tonumber()</code>.</p><p><strong>Persistence.</strong> Most effects are ordinary engine state and are saved with the campaign. These are <strong>not</strong>, and must be re-applied after every load:</p><div class=\"doc-table\"><table><thead><tr><th>Thing</th><th>What survives</th><th>What to do after a load</th></tr></thead><tbody><tr><td><code>se.modify.effect_bundle_define</code></td><td>nothing (the DB record is patched in memory for the session)</td><td>call <code>effect_bundle_define</code> again and re-apply the bundle</td></tr><tr><td><code>se.modify.autoresolver_variable</code></td><td>nothing</td><td>call it again (within a session the module re-applies values at the local player's <code>FactionTurnStart</code>, because the engine rebuilds the variable array when its per-round overrides change)</td></tr><tr><td><code>se.modify.autoresolve_plan</code></td><td>nothing; the plan is keyed to one pending battle and cleared on <code>BattleCompleted</code></td><td>set a new plan / install the handler again</td></tr><tr><td><code>se.modify.build_number</code></td><td>nothing (process-local UI strings)</td><td>set it again, or let the DLL apply <code>script_extender.cfg</code> at injection</td></tr><tr><td><code>se.modify.emperor_policy</code></td><td>the policy string (via <code>cm:save_named_value</code>); the listener does not</td><td><code>se.load_emperor_policy()</code></td></tr><tr><td><code>se.modify.faction_income</code></td><td>the income lines (via <code>cm:save_named_value</code>); the listener does not</td><td><code>se.load_income_lines()</code></td></tr><tr><td><code>se.autoresolve.set_handler</code></td><td>nothing (a Lua function)</td><td>install it again</td></tr><tr><td><code>se.ai_recruit.enable</code> and its tunables</td><td>nothing (Lua state)</td><td>set the tables and call <code>se.ai_recruit.enable()</code> again</td></tr><tr><td><code>se.diag.listeners_start</code></td><td>nothing (Lua state)</td><td>call it again if you still want the timings</td></tr></tbody></table></div><p>Verified to persist across save / restart / load: <code>cai_personality</code> and <code>faction_potential</code>. Not yet tested: persistence of forced world-leader seats, of a renamed alliance, and of the per-instance custom effect list from <code>effect_bundle_apply_custom</code>.</p><p><strong>Reading engine dumps.</strong> Several query tables carry an <code>engine</code> (or <code>engine_*</code>) field holding a raw diagnostic string from the DLL. It is for debugging and its format is not stable.</p><hr>"
        },
        {
          "level": 2,
          "id": "3-api-reference",
          "title": "3. API reference",
          "text": "3. API reference",
          "html": "<p>Notation: <code>(a, b [, c])</code> marks optional arguments. \"Status\" reflects <code>HANDOFF.md</code> and the notes at the time of writing.</p>"
        },
        {
          "level": 3,
          "id": "3-0-core-helpers",
          "title": "3.0 Core helpers",
          "text": "3.0 Core helpers",
          "html": ""
        },
        {
          "level": 4,
          "id": "se-version-string",
          "title": "<code>se.version() -&gt; string</code>",
          "text": "se.version() -> string",
          "html": "<p>DLL version string. Since 0.8 (returns <code>\"unknown (DLL older than 0.8)\"</code> on earlier builds).</p>"
        },
        {
          "level": 4,
          "id": "se-available-native-name-boolean",
          "title": "<code>se.available(native_name) -&gt; boolean</code>",
          "text": "se.available(native_name) -> boolean",
          "html": "<p>True when the given <code>se_*</code> native exists in this Lua state.</p>"
        },
        {
          "level": 4,
          "id": "se-on-model-tag-f-cb-ok-message",
          "title": "<code>se.on_model(tag, f [, cb]) -&gt; ok, message</code>",
          "text": "se.on_model(tag, f [, cb]) -> ok, message",
          "html": "<p>Run <code>f</code> on the model thread (see §1). <code>f</code> returns <code>ok, message</code>. <code>cb(ok, msg)</code> is optional. Refuses in multiplayer.</p>"
        },
        {
          "level": 4,
          "id": "se-log-s",
          "title": "<code>se.log(s)</code>",
          "text": "se.log(s)",
          "html": "<p>Log through <code>se.logger</code> / <code>ModLog</code> / <code>se_log</code>, prefixed <code>[se] </code>.</p>"
        },
        {
          "level": 4,
          "id": "se-is-null-v-boolean",
          "title": "<code>se.is_null(v) -&gt; boolean</code>",
          "text": "se.is_null(v) -> boolean",
          "html": "<p>True for anything that is not a live userdata interface.</p>"
        },
        {
          "level": 4,
          "id": "se-g-name-value",
          "title": "<code>se.G(name) -&gt; value</code>",
          "text": "se.G(name) -> value",
          "html": "<p>Look a global up in the state's real global table (the module chunk's <code>_G</code> is not the console's).</p>"
        },
        {
          "level": 4,
          "id": "se-character-cqi-query-character-nil-message",
          "title": "<code>se.character(cqi) -&gt; QUERY_CHARACTER | nil, message</code>",
          "text": "se.character(cqi) -> QUERY_CHARACTER | nil, message",
          "html": "<p><code>cm:query_character(cqi)</code> with a null check and a cqi round-trip check.</p>"
        },
        {
          "level": 4,
          "id": "se-faction-key-query-faction-nil-message",
          "title": "<code>se.faction(key) -&gt; QUERY_FACTION | nil, message</code>",
          "text": "se.faction(key) -> QUERY_FACTION | nil, message",
          "html": ""
        },
        {
          "level": 4,
          "id": "se-region-key-query-region-nil-message",
          "title": "<code>se.region(key) -&gt; QUERY_REGION | nil, message</code>",
          "text": "se.region(key) -> QUERY_REGION | nil, message",
          "html": ""
        },
        {
          "level": 4,
          "id": "se-dump-t-indent-string",
          "title": "<code>se.dump(t [, indent]) -&gt; string</code>",
          "text": "se.dump(t [, indent]) -> string",
          "html": "<p>Pretty-print a table (sorted keys, nested). Console convenience: <code>ModLog(se.dump(se.query.retinue(1)))</code>.</p><hr>"
        },
        {
          "level": 3,
          "id": "3-1-characters-and-recruitment-pools",
          "title": "3.1 Characters and recruitment pools",
          "text": "3.1 Characters and recruitment pools",
          "html": "<p>Engine background (verified live): a CHARACTER carries a state field — 0 = in the faction's recruitment pool, 1 = recruited, 2 = a third list. <code>move_to_faction</code> relinks by the character's <em>current</em> state, so it moves a pool character pool-to-pool and a recruited character recruited-to-recruited. The one primitive stock Lua lacks is <strong>recruited -&gt; pool</strong>, which is what <code>se_release_to_pool</code> provides.</p><p>The court's candidate list hides a pool character whose availability status byte is not 10; that byte (and its counter) is what <code>pool_lock</code> reads and writes. A character moved between factions is typically left locked (status 5) for a couple of rounds by the engine.</p>"
        },
        {
          "level": 4,
          "id": "se-query-character-cqi-table-nil-message",
          "title": "<code>se.query.character(cqi) -&gt; table | nil, message</code>",
          "text": "se.query.character(cqi) -> table | nil, message",
          "html": "<p>Returns <code>{ cqi, template, faction, in_pool, recruited, rank, experience, is_faction_leader, has_military_force, has_region, wounded [, engine] }</code>. <code>engine</code> is present when the native <code>se_char_info</code> exists.</p><p>Native: <code>se_char_info</code> (optional). Since 0.8. Status: verified live.</p><pre><code class=\"lang-lua\">ModLog(se.dump(se.query.character(72)))</code></pre>"
        },
        {
          "level": 4,
          "id": "se-query-pool-lock-cqi-status-counter-nil-message",
          "title": "<code>se.query.pool_lock(cqi) -&gt; status, counter | nil, message</code>",
          "text": "se.query.pool_lock(cqi) -> status, counter | nil, message",
          "html": "<p><code>status</code> 10 = available, 5 = locked; <code>counter</code> is the remaining round count. Native <code>se_pool_lock_get</code>, since 0.3. Status: verified live.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-pool-lock-cqi-turns-ok-message",
          "title": "<code>se.modify.pool_lock(cqi, turns) -&gt; ok, message</code>",
          "text": "se.modify.pool_lock(cqi, turns) -> ok, message",
          "html": "<p>Lock a pool character (status 5) for <code>turns</code> rounds (default 1). Native <code>se_pool_lock_set</code>. Model-thread queued. Saved with the campaign (ordinary character fields). The game clears the lock on its own turn tick. Status: verified live.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-pool-unlock-cqi-ok-message",
          "title": "<code>se.modify.pool_unlock(cqi) -&gt; ok, message</code>",
          "text": "se.modify.pool_unlock(cqi) -> ok, message",
          "html": "<p>Status 10, counter 0 — the character becomes selectable in the court. <strong>Reopen the court screen</strong> to see the list refresh. Status: verified live.</p><pre><code class=\"lang-lua\">se.modify.pool_unlock(176)</code></pre>"
        },
        {
          "level": 4,
          "id": "se-modify-release-to-pool-cqi-ok-message",
          "title": "<code>se.modify.release_to_pool(cqi) -&gt; ok, message</code>",
          "text": "se.modify.release_to_pool(cqi) -> ok, message",
          "html": "<p>Move a <strong>recruited</strong> character into their own faction's recruitment pool, through the engine's own release routine. Native <code>se_release_to_pool</code>.</p><p>Preconditions enforced by the engine: the faction must have a pool object, the character must have details, and the character must not hold an assignment post (a governor is refused). UI-level blockers (faction leader, commands or is embedded in a force, governs a region, has a character post, wounded) are worth checking yourself. <code>is_politician()</code> and a non-null <code>active_assignment()</code> are <strong>not</strong> valid blockers — every court member reports them.</p><p>Status: verified live (state 1 -&gt; 0, pool count +1, stable).</p>"
        },
        {
          "level": 4,
          "id": "se-modify-move-character-cqi-faction-key-state-ok-message",
          "title": "<code>se.modify.move_character(cqi, faction_key [, state]) -&gt; ok, message</code>",
          "text": "se.modify.move_character(cqi, faction_key [, state]) -> ok, message",
          "html": "<p>Move a character to <code>faction_key</code> (when they are not already there) and then force the requested list membership.</p><ul><li><code>state</code> = <code>\"pool\"</code> (default) or <code>\"recruited\"</code>. Anything else returns <code>false, \"state must be 'pool' or 'recruited'\"</code>.</li><li><code>\"pool\"</code> requires the native <code>se_release_to_pool</code>; <code>\"recruited\"</code> uses only stock calls.</li><li>When a faction move happened, the state step runs <strong>one second later</strong> through <code>cm:callback</code> (so the engine has relinked the character first) and reports into the log, not into the return value. From the in-game console <code>cm:callback</code> does not fire — do the second step yourself.</li></ul><p>Status: verified live for all three cases (pool -&gt; pool, recruited -&gt; own pool, another faction's recruited -&gt; our pool).</p><pre><code class=\"lang-lua\">-- Guan Yu (cqi 72) into Cao Cao's recruitment pool\nse.modify.move_character(72, \"3k_main_faction_cao_cao\", \"pool\")</code></pre><hr>"
        },
        {
          "level": 3,
          "id": "3-2-retinues-and-units",
          "title": "3.2 Retinues and units",
          "text": "3.2 Retinues and units",
          "html": "<p>Slot 0 of a persistent retinue is the commander and has no recruitment interface. <strong>A slot's recruitment interface recruits INTO that slot, replacing whatever unit is there</strong> — target an empty slot to add a unit. Every slot's item list holds every unit in the game's recruitment trees (~466 entries in the grand campaign, of which only a few dozen are unlocked); <code>reasons</code> is a bit mask of lock causes, <code>0</code> meaning unlocked.</p><p>Unit strength is hit-point scaled internally; the API works in percent of full strength. Strength and chevron writes are direct field writes confirmed by the stock getters and the retinue panel; the engine's own setters were never traced (an open item — derived state could in principle go stale).</p>"
        },
        {
          "level": 4,
          "id": "se-query-retinue-cqi-list-nil-message",
          "title": "<code>se.query.retinue(cqi) -&gt; list | nil, message</code>",
          "text": "se.query.retinue(cqi) -> list | nil, message",
          "html": "<p>One row per slot, sorted by slot index: <code>{ index, slot_cqi, unit_key, strength, experience, can_recruit, is_recruiting, recruiting }</code>. <code>strength</code> / <code>experience</code> are only filled when the slot is linked to a deployed military force unit. <code>slot_cqi</code> is the slot's own command-queue index — the number the stock <code>UnitRecruitmentInitiated</code> event reports as <code>context:cqi()</code>, which is how you match an event back to a slot. Since 0.8. Status: verified live.</p><p>Note: right after a campaign load, <code>retinue_slots()</code> entries can briefly read as null interfaces; read the list again a moment later.</p>"
        },
        {
          "level": 4,
          "id": "se-query-recruitable-cqi-slot-index-list-nil-message",
          "title": "<code>se.query.recruitable(cqi, slot_index) -&gt; list | nil, message</code>",
          "text": "se.query.recruitable(cqi, slot_index) -> list | nil, message",
          "html": "<p><code>{ { key, record, cost, turns, reasons }, ... }</code> for that slot. <code>reasons == 0</code> means the item is unlocked. Native <code>se_slot_items</code>, since 0.4. Status: verified live.</p><pre><code class=\"lang-lua\">local items = se.query.recruitable(1, 1) or {}\nfor _, it in ipairs(items) do\n    if it.reasons == 0 then ModLog(it.key .. \" cost=\" .. it.cost .. \" turns=\" .. it.turns) end\nend</code></pre>"
        },
        {
          "level": 4,
          "id": "se-query-unit-cqi-slot-index-table-nil-message",
          "title": "<code>se.query.unit(cqi, slot_index) -&gt; table | nil, message</code>",
          "text": "se.query.unit(cqi, slot_index) -> table | nil, message",
          "html": "<p><code>{ unit_key, strength, experience [, engine] [, strength_engine] }</code>. Since 0.8.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-recruit-cqi-unit-key-opts-ok-message",
          "title": "<code>se.modify.recruit(cqi, unit_key [, opts]) -&gt; ok, message</code>",
          "text": "se.modify.recruit(cqi, unit_key [, opts]) -> ok, message",
          "html": "<p>Start the engine's recruitment of <code>unit_key</code> into one of the character's retinue slots.</p><div class=\"doc-table\"><table><thead><tr><th>option</th><th>type</th><th>default</th><th>meaning</th></tr></thead><tbody><tr><td><code>source</code></td><td><code>\"unlocked\"</code> \\</td><td><code>\"locked\"</code> \\</td><td><code>\"any\"</code></td><td><code>\"unlocked\"</code></td><td><code>\"unlocked\"</code> refuses an item whose <code>reasons ~= 0</code>; <code>\"locked\"</code> refuses an unlocked item; <code>\"any\"</code> accepts either. Locked items are forced through by clearing the item's lock reasons.</td></tr><tr><td><code>free</code></td><td>boolean</td><td><code>false</code></td><td>zero the cost</td></tr><tr><td><code>slot</code></td><td>number</td><td>first empty slot that can recruit</td><td>slot index to recruit into; slot 0 is refused</td></tr><tr><td><code>replace</code></td><td>boolean</td><td><code>false</code></td><td>allow an occupied slot (its unit is replaced)</td></tr><tr><td><code>hp</code></td><td>0..100</td><td>none</td><td>strength percent applied 1 s after the recruit (needs <code>se_unit_strength_set</code>)</td></tr><tr><td><code>experience</code></td><td>number</td><td>none</td><td>unit experience level applied 1 s after the recruit (needs <code>se_unit_xp_set</code>)</td></tr></tbody></table></div><p>Refusals you will see: <code>unit_key must be a non-empty string</code>, <code>opts.hp must be 0..100</code>, <code>slot N holds &lt;key&gt;; pass replace = true to swap it</code>, <code>no empty slot that can recruit (pass opts.slot, or replace = true)</code>, <code>&lt;key&gt; is not in slot N's item list (M items)</code>, <code>&lt;key&gt; is locked (reasons 0x...); use opts.source = 'locked' or 'any'</code>.</p><p>Natives: <code>se_recruit_unit</code>, <code>se_slot_items</code> (since 0.4/0.5); <code>hp</code> / <code>experience</code> need 0.8. Status: verified live, including a locked unit (<code>reasons 0x402</code>) forced through, cost deducted, unit instantly in the slot.</p><pre><code class=\"lang-lua\">se.modify.recruit(1, \"3k_dlc04_unit_wood_imperial_gate_guards\",\n                  { source = \"any\", free = true, slot = 5, hp = 50 })</code></pre>"
        },
        {
          "level": 4,
          "id": "se-modify-replace-cqi-slot-index-unit-key-opts-ok-message",
          "title": "<code>se.modify.replace(cqi, slot_index, unit_key [, opts]) -&gt; ok, message</code>",
          "text": "se.modify.replace(cqi, slot_index, unit_key [, opts]) -> ok, message",
          "html": "<p>Shorthand for <code>se.modify.recruit</code> with <code>opts.slot = slot_index</code> and <code>opts.replace = true</code>. It writes those two keys <strong>into the table you pass</strong>, so do not reuse one <code>opts</code> table for a later <code>recruit</code> call unless you want the slot and the replace flag to come along.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-disband-cqi-slot-index-ok-message",
          "title": "<code>se.modify.disband(cqi, slot_index) -&gt; ok, message</code>",
          "text": "se.modify.disband(cqi, slot_index) -> ok, message",
          "html": "<p>Empty a retinue slot. Slot 0 is refused (<code>refusing to disband the commander's own slot</code>); an already empty slot is refused.</p><p>This is what the UI's Disband does: the slot's recruit command with an empty unit key, which the engine resolves to the slot's \"empty\" item. (The engine's <code>CCQ_DISBAND_UNIT</code> refuses retinue-slot units, so it is not used.) Native <code>se_recruit_unit</code>. Since 0.10. Status: verified live.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-unit-strength-cqi-slot-index-percent-ok-message",
          "title": "<code>se.modify.unit_strength(cqi, slot_index, percent) -&gt; ok, message</code>",
          "text": "se.modify.unit_strength(cqi, slot_index, percent) -> ok, message",
          "html": "<p>Set the unit's strength to <code>percent</code> of full strength. Native <code>se_unit_strength_set</code>, since 0.8. Saved with the campaign. Status: verified live (50% set, the stock getter agreed).</p>"
        },
        {
          "level": 4,
          "id": "se-modify-unit-experience-cqi-slot-index-level-ok-message",
          "title": "<code>se.modify.unit_experience(cqi, slot_index, level) -&gt; ok, message</code>",
          "text": "se.modify.unit_experience(cqi, slot_index, level) -> ok, message",
          "html": "<p>Set the unit's chevron level (the example scripts use 0..9; the API does not range-check). Native <code>se_unit_xp_set</code>, since 0.8. Status: implemented, confirmed by the stock getter and the retinue panel; the engine's own setter was never traced.</p><hr>"
        },
        {
          "level": 3,
          "id": "3-3-faction-progression-three-kingdoms-and-emperor-seats",
          "title": "3.3 Faction progression, Three Kingdoms and emperor seats",
          "text": "3.3 Faction progression, Three Kingdoms and emperor seats",
          "html": "<p>\"Emperor\" here means: the faction reached its top progression level and took a <strong>world-leader seat</strong>. Vanilla <code>3k_campaign_progression.lua</code> plays the Three Kingdoms movie once three seats exist. Seat count and maximum come from the engine's world-leader manager (3 in vanilla).</p>"
        },
        {
          "level": 4,
          "id": "se-query-faction-key-table-nil-message",
          "title": "<code>se.query.faction(key) -&gt; table | nil, message</code>",
          "text": "se.query.faction(key) -> table | nil, message",
          "html": "<p><code>{ key, level, max_level, level_key, is_world_leader, world_leader_regions, is_human, is_dead [, engine_level, engine_max, engine_level_key, engine_leader, locked | engine_error] [, leaders] }</code>.</p><p><code>leaders</code> is a string from the DLL of the form <code>used/total</code> seats (the module parses the total out of it). Natives: <code>se_faction_progression_get</code>, <code>se_world_leaders</code> (optional). Since 0.8. Status: verified live.</p>"
        },
        {
          "level": 4,
          "id": "se-query-world-leaders-faction-key-nil-message",
          "title": "<code>se.query.world_leaders() -&gt; { faction_key, ... } | nil, message</code>",
          "text": "se.query.world_leaders() -> { faction_key, ... } | nil, message",
          "html": "<p>Stock <code>is_world_leader()</code> over every faction in the world.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-faction-progression-key-level-ok-message",
          "title": "<code>se.modify.faction_progression(key, level) -&gt; ok, message</code>",
          "text": "se.modify.faction_progression(key, level) -> ok, message",
          "html": "<p>Raise a faction to progression <code>level</code> through the engine's own unlock + process path (prestige thresholds are rewritten, then the engine's own level transition runs, applying the level's effects and firing its events). At the maximum level the engine also tries to grant a world-leader seat.</p><p>Native <code>se_faction_progression_set</code>. Since 0.8. Status: verified live (three factions raised; vanilla script logged \"Emperor Seat Established\" for two of them).</p><p>Known limitation: factions on a Han-loyal \"governor\" progression group fail the engine's seat eligibility check and get the level but no seat — use <code>se.modify.world_leader</code> (or leave <code>force_three_kingdoms</code>'s <code>bypass</code> default) for them.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-world-leader-key-ok-message",
          "title": "<code>se.modify.world_leader(key) -&gt; ok, message</code>",
          "text": "se.modify.world_leader(key) -> ok, message",
          "html": "<p>Grant a world-leader seat directly, bypassing the eligibility check. Needs a free seat and a capital. Native <code>se_world_leader_force</code>. Since 0.10. Status: verified live.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-force-three-kingdoms-opts-ok-message",
          "title": "<code>se.modify.force_three_kingdoms([opts]) -&gt; ok, message</code>",
          "text": "se.modify.force_three_kingdoms([opts]) -> ok, message",
          "html": "<p>Fill the world-leader seats: forced factions first, then the highest-progression living factions that are not banned. Each pick is raised to its maximum progression level.</p><div class=\"doc-table\"><table><thead><tr><th>option</th><th>type</th><th>default</th><th>meaning</th></tr></thead><tbody><tr><td><code>forced</code></td><td>list of faction keys</td><td><code>{}</code></td><td>must take a seat, in order</td></tr><tr><td><code>banned</code></td><td>list of faction keys</td><td><code>{}</code></td><td>never auto-picked</td></tr><tr><td><code>include_human</code></td><td>boolean</td><td><code>false</code></td><td>allow the human faction to be auto-picked (<code>forced</code> always applies)</td></tr><tr><td><code>seats</code></td><td>number</td><td>the engine's maximum (3)</td><td>number of seats to fill</td></tr><tr><td><code>bypass</code></td><td>boolean</td><td><code>true</code></td><td>when the engine refuses a seat after the level change, seat the faction anyway through the world-leader manager</td></tr></tbody></table></div><p>Returns a multi-line report. Refuses with <code>would need N seats but only M exist</code> when the picks exceed the seat count, and with <code>forced faction '&lt;key&gt;' does not exist</code>.</p><p>Natives: <code>se_faction_progression_set</code>, <code>se_faction_progression_get</code>, plus <code>se_world_leader_force</code> for the bypass. Since 0.8/0.10. Status: verified live end to end — the vanilla script logged \"We Have Three Kingdoms!\" and played the movie. <strong>Save persistence of the seats has not been tested.</strong></p><pre><code class=\"lang-lua\">se.modify.force_three_kingdoms{\n    forced = { \"3k_main_faction_kong_rong\" },\n    banned = { \"3k_main_faction_dong_zhuo\" },\n}</code></pre>"
        },
        {
          "level": 4,
          "id": "se-modify-emperor-policy-policy-ok-message",
          "title": "<code>se.modify.emperor_policy(policy) -&gt; ok, message</code>",
          "text": "se.modify.emperor_policy(policy) -> ok, message",
          "html": "<p>Persistent steering of who may become emperor. <code>policy = { forced = {...}, banned = {...} }</code>, or <code>nil</code> to clear.</p><ul><li>The policy string is saved with <code>cm:save_named_value(\"se_emperor_policy\", ...)</code>.</li><li>It installs a <code>FactionTurnStart</code> listener (needs <code>se.core</code>): banned factions sitting one step below their maximum level get <code>lock_progression_level_changes()</code>; forced factions are promoted to their maximum level while seats are free.</li></ul><p>Status: implemented; <strong>the turn-start listener has not been observed across a turn yet.</strong></p>"
        },
        {
          "level": 4,
          "id": "se-load-emperor-policy-ok-message",
          "title": "<code>se.load_emperor_policy() -&gt; ok, message</code>",
          "text": "se.load_emperor_policy() -> ok, message",
          "html": "<p>Restore the saved policy and re-install the listener. Call it from a <code>LoadingGame</code> / first-tick hook. Returns <code>false, \"no saved policy\"</code> when nothing was stored.</p><hr>"
        },
        {
          "level": 3,
          "id": "3-4-experience-and-skill-points",
          "title": "3.4 Experience and skill points",
          "text": "3.4 Experience and skill points",
          "html": "<p>Engine background: a character's experience lives in their details block; adding experience runs a rank loop that awards skill points per rank. The engine's own \"scaled\" path multiplies the amount by the character's and the faction's experience-gain modifiers (effect id 385 / <code>0x181</code>).</p>"
        },
        {
          "level": 4,
          "id": "se-query-character-xp-cqi-table-nil-message",
          "title": "<code>se.query.character_xp(cqi) -&gt; table | nil, message</code>",
          "text": "se.query.character_xp(cqi) -> table | nil, message",
          "html": "<p><code>{ experience, rank [, engine_xp, engine_rank, max_rank, skill_points] [, dump] }</code>. The engine fields need <code>se_char_rank_get</code> (0.12); with only the older <code>se_char_xp_get</code> you get <code>engine_xp</code> and a raw <code>dump</code> string.</p>"
        },
        {
          "level": 4,
          "id": "se-query-skill-points-cqi-number-nil-message",
          "title": "<code>se.query.skill_points(cqi) -&gt; number | nil, message</code>",
          "text": "se.query.skill_points(cqi) -> number | nil, message",
          "html": "<p>Unspent skill points. Returns <code>nil, \"native se_char_rank_get is not available\"</code> on a DLL older than 0.12.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-character-add-xp-cqi-n-scaled-ok-message",
          "title": "<code>se.modify.character_add_xp(cqi, n [, scaled]) -&gt; ok, message</code>",
          "text": "se.modify.character_add_xp(cqi, n [, scaled]) -> ok, message",
          "html": "<p>Add experience. <code>n</code> must be a positive number.</p><ul><li><code>scaled</code> falsy (default): add <strong>exactly</strong> <code>n</code>; rank-ups are processed by the engine's own rank loop afterwards.</li><li><code>scaled = true</code>: go through the engine's scaled path, which multiplies <code>n</code> by the character's and faction's experience-gain modifiers.</li></ul><p>Native <code>se_char_xp_add</code>. Since 0.12. Saved with the campaign. Status: verified live (raw add of 100 moved 373 -&gt; 473; the stock <code>add_experience(1000, 0)</code> applied 1740 on the same character, which is the scaling this flag avoids).</p>"
        },
        {
          "level": 4,
          "id": "se-modify-skill-points-cqi-n-ok-message",
          "title": "<code>se.modify.skill_points(cqi, n) -&gt; ok, message</code>",
          "text": "se.modify.skill_points(cqi, n) -> ok, message",
          "html": "<p>Set the unspent skill-point counter (0..100). Native <code>se_skill_points_set</code>. Since 0.12.</p><p>Known gap: there is no \"grant skill by key\" — skills are not CEOs outside Nanman and the skill allocation executor has not been wrapped.</p>"
        },
        {
          "level": 4,
          "id": "se-query-faction-effect-value-faction-key-effect-id-number-nil-message",
          "title": "<code>se.query.faction_effect_value(faction_key, effect_id) -&gt; number | nil, message</code>",
          "text": "se.query.faction_effect_value(faction_key, effect_id) -> number | nil, message",
          "html": "<p>The faction-level value of an engine effect id (a float). Native <code>se_faction_effect_value</code>, since 0.12.</p>"
        },
        {
          "level": 4,
          "id": "se-query-faction-xp-gain-percent-faction-key-number-nil-message",
          "title": "<code>se.query.faction_xp_gain_percent(faction_key) -&gt; number | nil, message</code>",
          "text": "se.query.faction_xp_gain_percent(faction_key) -> number | nil, message",
          "html": "<p>Shorthand for effect id <code>se.EFFECT_CHARACTER_XP_GAIN</code> (385 / <code>0x181</code>), the faction's character experience gain percentage. This is the <strong>faction-level part only</strong>; a character's own effects are not included.</p><p>There is no effect id -&gt; key map yet; <code>385</code> is the only id the API names.</p><hr>"
        },
        {
          "level": 3,
          "id": "3-5-assignments",
          "title": "3.5 Assignments",
          "text": "3.5 Assignments",
          "html": ""
        },
        {
          "level": 4,
          "id": "se-query-assignment-cqi-dump-table-nil-message",
          "title": "<code>se.query.assignment(cqi [, dump]) -&gt; table | nil, message</code>",
          "text": "se.query.assignment(cqi [, dump]) -> table | nil, message",
          "html": "<p><code>{ key, state, rounds, idle [, province, state_index, transition_round] [, engine] }</code>.</p><ul><li><code>key</code> is the assignment record key, <code>state</code> the engine's state string, <code>rounds</code> the stock <code>rounds_until_state_transition()</code> (an idle assignment stores <code>0xffffffff</code>, so this reads as a huge number), <code>idle</code> the stock <code>is_idle_assignment()</code>.</li><li><code>province</code> (the key of the province the assignment is performed in, <code>\"\"</code> for idle assignments), <code>state_index</code> and <code>transition_round</code> need the native <code>se_assignment_info</code> (0.11).</li><li><code>dump == true</code> adds a raw <code>engine</code> dump when <code>se_assignment_dump</code> exists.</li></ul><p>Returns <code>nil, \"character has no active assignment\"</code> when there is none. Status: verified live (province read as <code>3k_main_province_yingchuan</code> on a real assignment).</p><p>There is no modify side for assignments.</p><hr>"
        },
        {
          "level": 3,
          "id": "3-6-campaign-ai-personality",
          "title": "3.6 Campaign AI personality",
          "text": "3.6 Campaign AI personality",
          "html": ""
        },
        {
          "level": 4,
          "id": "se-query-cai-personality-faction-key-key-record-key-nil-message",
          "title": "<code>se.query.cai_personality(faction_key) -&gt; { key, record_key } | nil, message</code>",
          "text": "se.query.cai_personality(faction_key) -> { key, record_key } | nil, message",
          "html": "<p>The faction's current CAI personality. <strong>Must run where <code>cm:can_modify()</code> is true</strong> (the CAI manager comes from <code>cm:modify_campaign_ai()</code>); from the console, wrap it in <code>se.on_model</code>. Native <code>se_cai_personality_get</code>, since 0.12.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-cai-personality-faction-key-personality-key-ok-message",
          "title": "<code>se.modify.cai_personality(faction_key, personality_key) -&gt; ok, message</code>",
          "text": "se.modify.cai_personality(faction_key, personality_key) -> ok, message",
          "html": "<p>Swap an AI faction's personality to any <code>cai_personalities</code> key. Native <code>se_cai_personality_set</code>. Since 0.12 (corrected in 0.13/0.14/0.15). Status: <strong>verified live, and persistence across save / restart / load verified.</strong></p><pre><code class=\"lang-lua\">se.on_model(\"read personality\", function()\n    ModLog(se.dump(se.query.cai_personality(\"3k_main_faction_dong_zhuo\") or {}))\n    return true, \"read\"\nend)\nse.modify.cai_personality(\"3k_main_faction_dong_zhuo\",\n                          \"3k_cai_personality_tao_qian_early_hard\")</code></pre><hr>"
        },
        {
          "level": 3,
          "id": "3-7-faction-potential",
          "title": "3.7 Faction potential",
          "text": "3.7 Faction potential",
          "html": "<p>The AI handicap rating: a faction's potential is <code>base + bonus + roll</code> (0 for human factions) and selects handicap effect rows from the database.</p>"
        },
        {
          "level": 4,
          "id": "se-query-faction-potential-key-value-base-bonus-roll-nil-message",
          "title": "<code>se.query.faction_potential(key) -&gt; { value, base, bonus, roll } | nil, message</code>",
          "text": "se.query.faction_potential(key) -> { value, base, bonus, roll } | nil, message",
          "html": "<p>Native <code>se_faction_potential_get</code>, since 0.12.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-faction-potential-key-value-ok-message",
          "title": "<code>se.modify.faction_potential(key, value) -&gt; ok, message</code>",
          "text": "se.modify.faction_potential(key, value) -> ok, message",
          "html": "<p>Set an AI faction's potential (range <strong>-100..150</strong>) by rewriting its base and re-applying the handicap effects through the engine. Native <code>se_faction_potential_set</code>. Status: <strong>verified live, persistence verified</strong> (103 -&gt; 120 held across save / restart / load).</p><hr>"
        },
        {
          "level": 3,
          "id": "3-8-buildings-and-region-slots",
          "title": "3.8 Buildings and region slots",
          "text": "3.8 Buildings and region slots",
          "html": "<p>Slot indices are positions in the region's <code>slot_list()</code>. All of these are model-thread queued. Since 0.19; forced construction since 0.23.</p>"
        },
        {
          "level": 4,
          "id": "se-query-region-slots-region-key-list-nil-message",
          "title": "<code>se.query.region_slots(region_key) -&gt; list | nil, message</code>",
          "text": "se.query.region_slots(region_key) -> list | nil, message",
          "html": "<p>One row per slot: <code>{ index, name, type, has_building [, building, chain, health] [, engine_key, engine_health, can_damage, engine] }</code>.</p>"
        },
        {
          "level": 4,
          "id": "se-query-building-candidates-region-key-slot-index-opts-level-key",
          "title": "<code>se.query.building_candidates(region_key, slot_index [, opts]) -&gt; { level_key, ... }</code>",
          "text": "se.query.building_candidates(region_key, slot_index [, opts]) -> { level_key, ... }",
          "html": "<p>Building levels the slot can hold.</p><ul><li>default: every level the slot's current chain set offers, <strong>blocked ones included</strong>;</li><li><code>opts.only_valid = true</code>: only what the UI would let you build right now. This list goes <strong>empty as soon as the slot has any blocking reason</strong>, which is normal, not an error;</li><li><code>opts.all_chains = true</code>: every chain the slot can hold.</li></ul><p>Native <code>se_slot_candidates</code>. Status: verified live.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-building-damage-region-key-slot-index-percent-ok-message",
          "title": "<code>se.modify.building_damage(region_key, slot_index, percent) -&gt; ok, message</code>",
          "text": "se.modify.building_damage(region_key, slot_index, percent) -> ok, message",
          "html": "<p>Damage the building by <code>percent</code>. Only works on buildings the database marks as damageable. Native <code>se_slot_damage</code>. Status: verified live (100 -&gt; 60).</p>"
        },
        {
          "level": 4,
          "id": "se-modify-building-repair-region-key-slot-index-opts-ok-message",
          "title": "<code>se.modify.building_repair(region_key, slot_index [, opts]) -&gt; ok, message</code>",
          "text": "se.modify.building_repair(region_key, slot_index [, opts]) -> ok, message",
          "html": "<p><code>opts.free = true</code> repairs directly with no cost; the default issues the engine's repair command (charges like the UI). Native <code>se_slot_repair</code>. Status: verified live (60 -&gt; 100 free).</p>"
        },
        {
          "level": 4,
          "id": "se-modify-building-destroy-region-key-slot-index-ok-message",
          "title": "<code>se.modify.building_destroy(region_key, slot_index) -&gt; ok, message</code>",
          "text": "se.modify.building_destroy(region_key, slot_index) -> ok, message",
          "html": "<p>Native <code>se_slot_destroy</code>. Status: verified live.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-building-construct-region-key-slot-index-level-key-opts-ok-message",
          "title": "<code>se.modify.building_construct(region_key, slot_index, level_key [, opts]) -&gt; ok, message</code>",
          "text": "se.modify.building_construct(region_key, slot_index, level_key [, opts]) -> ok, message",
          "html": "<p>Start construction of <code>level_key</code> in the slot. An <strong>upgrade or a conversion is the same call with the target level key</strong>.</p><div class=\"doc-table\"><table><thead><tr><th>option</th><th>type</th><th>default</th><th>meaning</th></tr></thead><tbody><tr><td><code>force</code></td><td>boolean</td><td><code>true</code></td><td>ignore the engine's blocking reasons (cost, requirements, siege). <code>force = false</code> behaves like the UI button.</td></tr><tr><td><code>any_chain</code></td><td>boolean</td><td><code>true</code></td><td>when the key is not an upgrade of the current building, retry the lookup across every chain the slot can hold</td></tr><tr><td><code>free</code></td><td>boolean</td><td><code>false</code></td><td>zero the cost; anything the treasury still lost is refunded afterwards</td></tr><tr><td><code>turns</code></td><td>number</td><td>engine value</td><td>construction time in turns</td></tr><tr><td><code>complete</code></td><td>boolean</td><td><code>false</code></td><td>shorthand for <code>turns = 1</code></td></tr><tr><td><code>pay_to_complete</code></td><td>boolean</td><td><code>false</code></td><td>also issue the engine's pay-to-complete-next-turn command</td></tr></tbody></table></div><p>Returns a report string (<code>...; refunded N</code> / <code>pay_to_complete -&gt; ...</code>). Native <code>se_slot_construct</code> (+ <code>se_slot_pay_to_complete</code>).</p><p>Status and known limitations:</p><ul><li>verified live: forced free upgrade of a city and of a resource building (cost zeroed, turns overridden to 1) <strong>completed at the next turn start</strong>;</li><li>a forced <strong>new</strong> building issued into an empty slot in the same province did <strong>not</strong> complete: after the turn the construction item had been dropped. Both entries carried the \"constructions in progress in the province &gt;= the province construction limit\" reason bit. Working theory: turn processing keeps only <code>limit</code> constructions and discards the rest. Force a build while the province is under its limit;</li><li>there is no true same-turn completion: <code>turns = 1</code> completes at the next turn start;</li><li>right after issuing, the slot still reports the old building — the construction sits in the slot's manager until it completes;</li><li>the call refuses when a construction is already in progress in that slot.</li></ul><hr>"
        },
        {
          "level": 3,
          "id": "3-9-alliances-and-coalitions",
          "title": "3.9 Alliances and coalitions",
          "text": "3.9 Alliances and coalitions",
          "html": ""
        },
        {
          "level": 4,
          "id": "se-query-alliances-list-nil-message",
          "title": "<code>se.query.alliances() -&gt; list | nil, message</code>",
          "text": "se.query.alliances() -> list | nil, message",
          "html": "<p><code>{ { cqi, name, members = { faction_key, ... } [, engine] }, ... }</code> over the world's alliance list. <code>name</code> needs the native <code>se_alliance_info</code> (0.20).</p>"
        },
        {
          "level": 4,
          "id": "se-modify-alliance-name-cqi-text-mode-ok-message",
          "title": "<code>se.modify.alliance_name(cqi, text [, mode]) -&gt; ok, message</code>",
          "text": "se.modify.alliance_name(cqi, text [, mode]) -> ok, message",
          "html": "<p>Rename an alliance / coalition. <code>mode</code> is <code>\"inline\"</code> (default) or <code>\"pointer\"</code> — two ways of storing the string on the engine object; <code>\"inline\"</code> is the likelier serialised field. Native <code>se_alliance_name_set</code>. Since 0.20.</p><p>Status: applied live and still in place after a turn; <strong>save persistence not yet tested</strong> — check it with a save/load before shipping it in a mod.</p><pre><code class=\"lang-lua\">for _, a in ipairs(se.query.alliances() or {}) do\n    for _, m in ipairs(a.members) do\n        if m == \"3k_main_faction_yuan_shao\" then\n            se.modify.alliance_name(a.cqi, \"Yuan Shao's Grand Coalition\")\n        end\n    end\nend</code></pre><hr>"
        },
        {
          "level": 3,
          "id": "3-10-effect-bundles",
          "title": "3.10 Effect bundles",
          "text": "3.10 Effect bundles",
          "html": "<p>An effect list is given as <code>{ { effect = \"&lt;effects key&gt;\", scope = \"&lt;campaign_effect_scopes key&gt;\", value = &lt;number&gt; }, ... }</code>. Every row needs all three fields, and the list must be non-empty, otherwise the call returns <code>effects must be a non-empty list of {effect=, scope=, value=}</code> or <code>effects[i] needs effect (string), scope (string) and value (number)</code>.</p><p>In all three functions <code>faction_key</code> only provides the model to the native; it defaults to <code>cm:get_local_faction()</code>.</p>"
        },
        {
          "level": 4,
          "id": "se-query-effect-bundle-bundle-key-faction-key-count-dump-nil-message",
          "title": "<code>se.query.effect_bundle(bundle_key [, faction_key]) -&gt; { count, dump } | nil, message</code>",
          "text": "se.query.effect_bundle(bundle_key [, faction_key]) -> { count, dump } | nil, message",
          "html": "<p>Engine-side inspection of an <code>effect_bundles</code> record: the number of effect entries and a raw dump. Native <code>se_effect_bundle_info</code>. Since 0.21.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-effect-bundle-define-bundle-key-effects-faction-key-ok-message",
          "title": "<code>se.modify.effect_bundle_define(bundle_key, effects [, faction_key]) -&gt; ok, message</code>",
          "text": "se.modify.effect_bundle_define(bundle_key, effects [, faction_key]) -> ok, message",
          "html": "<p>Replace the effect list of an <strong>existing</strong> <code>effect_bundles</code> record for this game session. Every later stock <code>apply_effect_bundle(bundle_key, ...)</code> on any holder (faction, character, region, force, ...) carries the new effects. Bundles applied <em>earlier</em> keep what they had until they are removed and applied again.</p><p><strong>Not saved.</strong> Define again after every load (first tick) and re-apply. Native <code>se_effect_bundle_define</code>. Since 0.23. Status: verified live (define followed by the stock <code>apply_effect_bundle</code> returned cleanly).</p>"
        },
        {
          "level": 4,
          "id": "se-modify-effect-bundle-restore-bundle-key-faction-key-ok-message",
          "title": "<code>se.modify.effect_bundle_restore(bundle_key [, faction_key]) -&gt; ok, message</code>",
          "text": "se.modify.effect_bundle_restore(bundle_key [, faction_key]) -> ok, message",
          "html": "<p>Put the stock effect list back. Native <code>se_effect_bundle_restore</code>. Since 0.23.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-effect-bundle-apply-custom-faction-key-bundle-key-effects-turns-ok-message",
          "title": "<code>se.modify.effect_bundle_apply_custom(faction_key, bundle_key, effects [, turns]) -&gt; ok, message</code>",
          "text": "se.modify.effect_bundle_apply_custom(faction_key, bundle_key, effects [, turns]) -> ok, message",
          "html": "<p>Apply <code>bundle_key</code> to the faction with its <strong>own</strong> per-instance effect list (the engine's custom list); the database record and every other holder of the bundle are untouched. <code>turns = 0</code> (the default) is permanent.</p><p>Native <code>se_effect_bundle_apply_custom</code>. Since 0.23 (corrected through 0.23.3). Status: verified live — the instance rebuilt with one active effect, the faction's bundle count went up and <code>has_effect_bundle</code> reported true. <strong>Save persistence of the custom list has not been tested.</strong></p><p>Open behaviour: a defined <code>+50 gdp_mod_all / faction_to_region_own</code> bundle did <strong>not</strong> move <code>projected_net_income</code>, before or after a turn. Whether that scope reaches the income computation is unresolved — test the effect you intend to use.</p><pre><code class=\"lang-lua\">se.modify.effect_bundle_apply_custom(\"3k_main_faction_cao_cao\",\n    \"3k_dlc05_effect_bundle_yellow_turban_economy\",\n    { { effect = \"3k_main_effect_characters_experience_bonus\",\n        scope  = \"faction_to_character_own_factionwide_unseen\", value = 25 } }, 5)</code></pre><hr>"
        },
        {
          "level": 3,
          "id": "3-11-diplomacy-attitude",
          "title": "3.11 Diplomacy attitude",
          "text": "3.11 Diplomacy attitude",
          "html": ""
        },
        {
          "level": 4,
          "id": "se-query-attitude-a-b-standing-stock-nil-message",
          "title": "<code>se.query.attitude(a, b) -&gt; { standing, stock } | nil, message</code>",
          "text": "se.query.attitude(a, b) -> { standing, stock } | nil, message",
          "html": "<p>Standing of faction <code>a</code> towards faction <code>b</code>. <code>stock</code> is the stock <code>diplomatic_standing_with()</code>; <code>standing</code> is the engine's own computation, present when the native <code>se_attitude_get</code> exists. The attitude is computed from CAI components, not stored as a number.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-attitude-a-b-level-ok-message",
          "title": "<code>se.modify.attitude(a, b, level) -&gt; ok, message</code>",
          "text": "se.modify.attitude(a, b, level) -> ok, message",
          "html": "<p>Fire the engine's attitude-change event from <code>a</code> towards <code>b</code>. <code>level</code> = 1 / 2 / 3 (small / medium / large positive) and -1 / -2 / -3 (negative). The exact amounts come from the database's attitude-event records, so a mod retunes those rows rather than passing a number here.</p><p>Native <code>se_attitude_change</code>. Since 0.22. Status: the read-only side was verified live in a batch test; <strong>the change call is implemented but not confirmed live</strong>. Per-treaty-component evaluation bias is not implemented.</p><hr>"
        },
        {
          "level": 3,
          "id": "3-12-income-lines",
          "title": "3.12 Income lines",
          "text": "3.12 Income lines",
          "html": "<p>Script-side, not an engine income category: the engine's income breakdown has no slot for an arbitrary named line, so the amount is paid into the treasury at the faction's turn start and does <strong>not</strong> appear in the income breakdown or in <code>projected_net_income</code>.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-faction-income-faction-key-amount-label-ok-message",
          "title": "<code>se.modify.faction_income(faction_key, amount [, label]) -&gt; ok, message</code>",
          "text": "se.modify.faction_income(faction_key, amount [, label]) -> ok, message",
          "html": "<p>Add or replace a per-turn income line. <code>label</code> defaults to <code>\"se_income\"</code>; <code>amount = 0</code> (or <code>nil</code>) removes the line. Negative amounts are subtracted from the treasury.</p><p>Needs <code>se.core</code> — it installs a <code>FactionTurnStart</code> listener that pays the sum of that faction's lines with <code>increase_treasury</code> / <code>decrease_treasury</code>. The lines are saved with <code>cm:save_named_value(\"se_income_lines\", ...)</code>; the listener is not, so call <code>se.load_income_lines()</code> after a load.</p><p>This function does not go through <code>se.on_model</code> and does not itself check multiplayer.</p><p>Since 0.22 (module-side; no native needed). Status: verified live — a 1500 line was paid over a turn.</p>"
        },
        {
          "level": 4,
          "id": "se-query-faction-income-faction-key-label-amount-total-n",
          "title": "<code>se.query.faction_income(faction_key) -&gt; { [label] = amount, ..., total = n }</code>",
          "text": "se.query.faction_income(faction_key) -> { [label] = amount, ..., total = n }",
          "html": "<p>The lines currently known to this session.</p>"
        },
        {
          "level": 4,
          "id": "se-load-income-lines-ok-message",
          "title": "<code>se.load_income_lines() -&gt; ok, message</code>",
          "text": "se.load_income_lines() -> ok, message",
          "html": "<p>Restore saved lines and install the listener. <code>false, \"no saved income lines\"</code> when nothing was stored.</p><hr>"
        },
        {
          "level": 3,
          "id": "3-12a-horde-income-dll-0-28-on-by-default-since-0-37-0-beta-4-stable-0-40-0",
          "title": "3.12a Horde income (DLL 0.28+; on by default since 0.37.0-beta.4 / stable 0.40.0)",
          "text": "3.12a Horde income (DLL 0.28+; on by default since 0.37.0-beta.4 / stable 0.40.0)",
          "html": "<p><code>gdp_abs</code> effects (bonus value <code>region_gdp</code>) only reach the treasury through regions. The DLL hooks the engine routine that recomputes a faction's income categories (always, unless <code>script_extender.cfg</code> says <code>horde_income=0</code>; before 0.37.0-beta.4 it needed <code>horde_income=1</code>) and adds, 1:1, every <code>gdp_abs</code> value found on the <strong>faction itself</strong> (scopes such as <code>faction_to_faction_own</code>) and on <strong>each military force it owns</strong> (<code>force_to_force_own</code>, what horde building bundles use). <code>gdp_mod</code> values are ignored. Ordinary factions carry no such values (only percentage entries, which are ignored), so without a mod that grants them the amount is 0 and nothing changes. <code>horde_income_category</code> selects the category: <code>0</code> TAXES, <code>1</code> MINING (the default since 0.37.0-beta.4; <code>0</code> before), <code>2</code> TRADE, <code>3</code> MILITARY_FORCE. <strong>MINING is unused in 3K</strong> (never computed, no row in the stock treasury panel, but part of the totals), so a UI mod can show it as its own line: bind a treasury row to <code>CcoFactionEconomy</code> / <code>VaryingRegIncomeDetailsSum(\"MINING\")</code> and label it with <code>Loc(\"&lt;your key&gt;\")</code> (= <code>campaign_localised_strings_string_&lt;your key&gt;</code>), which other mods can translate. Both settings are part of the multiplayer version lock (their effective values, so an absent key and an explicit default give the same tag). Verified live on 0.28.3 / 0.29.1 (a 150 <code>gdp_abs</code> army bundle raised <code>projected_net_income</code> by 150); receiving it at turn end and a region-less faction are not yet confirmed.</p>"
        },
        {
          "level": 4,
          "id": "se-query-horde-income-hook-0-30-1",
          "title": "<code>se.query.horde_income_hook()</code> (0.30.1+)",
          "text": "se.query.horde_income_hook() (0.30.1+)",
          "html": "<p>Returns <code>{ enabled = boolean, category = 0..3, category_name = \"TAXES\" | \"MINING\" | \"TRADE\" | \"MILITARY_FORCE\" }</code>, or <code>nil, message</code> on an older DLL. <code>enabled</code> is the state of the detour in this process (cfg flag set and the hook installed), not the cfg text. Read-only, no model access, usable anywhere.</p><pre><code class=\"lang-lua\">local h = se.query.horde_income_hook()\nif not (h and h.enabled and h.category == 1) then\n\tModLog(\"horde income is off or points elsewhere: check horde_income / horde_income_category in script_extender.cfg (DLL older than 0.37.0-beta.4 needs horde_income=1, horde_income_category=1)\")\nend</code></pre>"
        },
        {
          "level": 4,
          "id": "se-query-faction-force-gdp-faction-key-0-28",
          "title": "<code>se.query.faction_force_gdp(faction_key)</code> (0.28+)",
          "text": "se.query.faction_force_gdp(faction_key) (0.28+)",
          "html": "<p>Returns <code>{ total = number, entries = string }</code>: the <code>gdp_abs</code> total the hook would add for that faction and one line per contributing entry (<code>faction</code> or <code>force[i]</code>). Works with the hook off.</p>"
        },
        {
          "level": 3,
          "id": "3-13-menu-build-number",
          "title": "3.13 Menu build number",
          "text": "3.13 Menu build number",
          "html": "<p>The main menu's build-number strings. Process-local UI state, not campaign state: <strong>no model thread, no multiplayer check, nothing saved.</strong> The DLL can also apply these at injection time from <code>script_extender.cfg</code> next to it (<code>build_number=</code>, <code>build_number_short=</code>, <code>build_modified=</code>).</p>"
        },
        {
          "level": 4,
          "id": "se-query-build-number-build-short-modified-nil-message",
          "title": "<code>se.query.build_number() -&gt; { build, short, modified } | nil, message</code>",
          "text": "se.query.build_number() -> { build, short, modified } | nil, message",
          "html": ""
        },
        {
          "level": 4,
          "id": "se-modify-build-number-build-short-modified-ok-message",
          "title": "<code>se.modify.build_number(build, short, modified) -&gt; ok, message</code>",
          "text": "se.modify.build_number(build, short, modified) -> ok, message",
          "html": "<p><code>\"\"</code> or <code>nil</code> for <code>build</code> / <code>short</code> keeps the existing string; <code>modified</code> is the \"build modified\" flag (pass <code>nil</code> to keep it). Natives <code>se_build_info_get</code> / <code>se_build_info_set</code>. Since 0.16 (corrected in 0.17, deferred apply in 0.18). Status: verified live through the mod manager.</p><pre><code class=\"lang-lua\">se.modify.build_number(nil, \"1.7.2.0 SE 1.0\", nil)</code></pre><hr>"
        },
        {
          "level": 3,
          "id": "3-14-auto-resolve",
          "title": "3.14 Auto-resolve",
          "text": "3.14 Auto-resolve",
          "html": "<p>Three separate mechanisms, in increasing order of intrusiveness:</p><ol><li><strong>Tunables</strong> — the auto-resolver's own constants (<code>autoresolver_*</code> campaign variables, ~85 of them, including 7 <code>autoresolver_duel_*</code>). Session-only.</li><li><strong>Read-out</strong> — the pending battle's context and the engine's prediction.</li><li><strong>Plans</strong> — a per-battle instruction the DLL applies to the freshly computed result.</li></ol><p>Everything here applies to battles a human player is involved in (every machine of a multiplayer game evaluates the same handler; see the multiplayer rules in §1).</p>"
        },
        {
          "level": 4,
          "id": "what-each-dll-version-actually-applies",
          "title": "What each DLL version actually applies",
          "text": "What each DLL version actually applies",
          "html": "<div class=\"doc-table\"><table><thead><tr><th>Plan field</th><th>Status</th></tr></thead><tbody><tr><td><code>winner</code></td><td>applied by the engine hook; <strong>use 0.26.2 or later</strong> (verified live there: winning alliance index and per-side result ids are written; 0.26.0 / 0.26.1 had no effect on the winner)</td></tr><tr><td><code>casualties</code></td><td>applied since 0.26; <strong>verified live</strong> (a 10% cap turned a predicted 798 -&gt; 563 into 798 -&gt; 730, and the army kept about 90% after the battle)</td></tr><tr><td><code>duels</code></td><td>applied since <strong>0.27</strong>; <strong>verified live</strong> (a forced winner flipped the game log from \"Proposer Won Duel\" to the other character). <code>default = \"none\"</code> removes every duel without a rule, <code>max</code> trims the list, a pair the engine did not roll is appended when both hero unit keys are known</td></tr><tr><td><code>duels[i].fate</code></td><td>accepted and validated — <strong>not applied</strong>: the campaign rolls the loser's wound or death afterwards</td></tr><tr><td><code>bias</code></td><td>accepted, clamped and stored — <strong>not applied</strong> by any current version</td></tr><tr><td><code>refresh_prediction</code></td><td>applied (re-runs the engine compute routine so the panel prediction matches)</td></tr></tbody></table></div><p>Further limitations of the result rewrite:</p><ul><li><strong>A side made of more than one army record is skipped</strong> (the element size of that vector is not mapped). A defender built from two forces was only partly covered in 0.26.0; 0.26.1 skips neutral sides entirely and moves a summary only by the delta of the units it actually rewrote.</li><li>Single-man records (characters) are never made <em>worse</em> by casualty or winner rules.</li><li>The pre-battle panel's casualty bar does not refresh after a recompute (the UI caches the prediction it read when the panel opened). Cosmetic.</li><li>Plans are keyed to the pending-battle object that existed when the plan was set; every other battle is ignored.</li><li>The engine's own auto-resolve simulation is deterministic: it re-runs on the auto-resolve click, which is why the DLL hooks the computation instead of rewriting a stored prediction.</li></ul>"
        },
        {
          "level": 4,
          "id": "se-query-autoresolver-variable-key-number-nil-message",
          "title": "<code>se.query.autoresolver_variable(key) -&gt; number | nil, message</code>",
          "text": "se.query.autoresolver_variable(key) -> number | nil, message",
          "html": "<p>One campaign variable. Only keys starting with <code>autoresolver_</code> are accepted.</p>"
        },
        {
          "level": 4,
          "id": "se-query-autoresolver-variables-key-value-nil-message",
          "title": "<code>se.query.autoresolver_variables() -&gt; { key = value, ... } | nil, message</code>",
          "text": "se.query.autoresolver_variables() -> { key = value, ... } | nil, message",
          "html": "<p>Every <code>autoresolver_*</code> key with its current value.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-autoresolver-variable-key-value-ok-message",
          "title": "<code>se.modify.autoresolver_variable(key, value) -&gt; ok, message</code>",
          "text": "se.modify.autoresolver_variable(key, value) -> ok, message",
          "html": "<p>Retune one constant for this session. <strong>Not saved</strong> — call it again after a load. Within a session, the module remembers the value and re-applies it at the local player's <code>FactionTurnStart</code> (needs <code>se.core</code>), because the engine rebuilds the variable array when its per-round overrides change.</p><p>Native <code>se_ar_variable_set</code>. Since 0.24. Status: verified live (reads matched the database values, writes took).</p><pre><code class=\"lang-lua\">se.modify.autoresolver_variable(\"autoresolver_duel_base_chance\", 1.0)  -- vanilla 0.5\nse.modify.autoresolver_variable(\"autoresolver_duel_max_limit\", 2)      -- vanilla 6</code></pre>"
        },
        {
          "level": 4,
          "id": "se-modify-autoresolver-variables-reset-ok-message",
          "title": "<code>se.modify.autoresolver_variables_reset() -&gt; ok, message</code>",
          "text": "se.modify.autoresolver_variables_reset() -> ok, message",
          "html": "<p>Every variable back to what the engine had before the first script write.</p>"
        },
        {
          "level": 4,
          "id": "se-query-autoresolve-prediction-table-nil-message",
          "title": "<code>se.query.autoresolve_prediction() -&gt; table | nil, message</code>",
          "text": "se.query.autoresolve_prediction() -> table | nil, message",
          "html": "<p>The engine's own prediction for the pending battle (what the pre-battle panel shows). Keys, all flat in one table:</p><p><code>available</code> (boolean), <code>pending_battle</code>, <code>night</code>, <code>result_index</code>, <code>results</code>, and per side (<code>attacker</code> / <code>defender</code>): <code>&lt;side&gt;_prediction</code> (one of <code>close_victory</code>, <code>decisive_victory</code>, <code>heroic_victory</code>, <code>pyrrhic_victory</code>, <code>draw</code>, <code>close_defeat</code>, <code>decisive_defeat</code>, <code>crushing_defeat</code>, <code>valiant_defeat</code>), <code>&lt;side&gt;_prediction_id</code> (0..8 in that order), <code>&lt;side&gt;_casualties_percent</code>, <code>&lt;side&gt;_strength_share</code>, <code>&lt;side&gt;_men_before</code>, <code>&lt;side&gt;_men_after</code>, <code>&lt;side&gt;_men_lost</code>.</p><p>Native <code>se_ar_prediction</code>. Since 0.24.</p>"
        },
        {
          "level": 4,
          "id": "se-query-pending-battle-table-nil-message",
          "title": "<code>se.query.pending_battle() -&gt; table | nil, message</code>",
          "text": "se.query.pending_battle() -> table | nil, message",
          "html": "<pre><code>{ active, battle_type, is_siege, is_ambush, is_night, human_involved,\n  local_player_involved,\n  attacker = { faction, is_human, is_local_player, strength,\n               forces     = { { cqi, general_cqi, units }, ... },\n               characters = { { cqi, faction, template, rank }, ... } },\n  defender = { ...same... },\n  prediction = se.query.autoresolve_prediction() }</code></pre><p>Returns <code>nil, \"no pending battle interface\"</code> when there is none. Uses only stock interfaces plus the prediction native.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-autoresolve-plan-plan-ctx-ok-message",
          "title": "<code>se.modify.autoresolve_plan(plan [, ctx]) -&gt; ok, message</code>",
          "text": "se.modify.autoresolve_plan(plan [, ctx]) -> ok, message",
          "html": "<p>Store a plan for the current pending battle. <code>ctx</code> defaults to <code>se.query.pending_battle()</code>; the call refuses with <code>refused: no pending battle with a human player</code> when <code>ctx.human_involved</code> is not true. Since 0.30 the test is \"a human is involved\", not \"the local player is involved\", so that every machine of a multiplayer game sets the same plan for the same battle; the model step itself still goes through <code>se.on_model</code> and is refused outside a model callback in multiplayer.</p><p>Plan shape (schema, not runnable code — <code>|</code> means \"one of\"):</p><pre><code class=\"lang-text\">plan = {\n  winner = \"attacker\" | \"defender\" | nil,       -- force the winner\n  casualties = {                                 -- per side\n     attacker = { scale = 1.0, max = 1.0 },      -- scale 0..10, max 0..1 (fraction lost)\n     defender = { scale = 1.0, max = 1.0 },\n  },\n  bias  = { attacker = 1.0, defender = 1.0 },    -- 0.1..10, STORED BUT NOT APPLIED\n  duels = {                                      -- applied since 0.27\n     max = 6,                                    -- 0..16, trims the duel list\n     default = \"vanilla\" | \"none\",               -- \"none\" drops every duel without a rule\n     pairs = { { a = cqi, b = cqi, happen = true, win_chance = 0.5, winner = cqi,\n                 fate = \"kill\" | \"wound\" | \"spare\" | \"flee\",   -- fate is NOT applied\n                 a_key = \"unit key\", b_key = \"unit key\" } },   -- only to create a missing duel\n  },\n  refresh_prediction = true,                     -- false skips the immediate panel recompute\n}</code></pre><p>Validation errors: <code>plan must be a table</code>, <code>plan.winner must be 'attacker', 'defender' or nil</code>, <code>plan.duels.default must be 'vanilla' or 'none'</code>, <code>plan.duels.pairs[i] needs character cqis a and b</code>, <code>plan.duels.pairs[i].fate must be kill, wound, spare or flee</code>. Numeric values outside their range are clamped rather than rejected.</p><p>A pair naming two characters the engine did not pair up is <strong>created</strong>, but only when both hero unit keys are known. The module resolves them for you when the character's force holds exactly one <code>_hero_</code> unit; when it holds two or more (a force with several generals), pass <code>a_key</code> / <code>b_key</code> yourself. <code>win_chance</code> is a probability, so in multiplayer derive the roll from <code>ctx.seed</code> (see §1) and never from <code>math.random</code>.</p><p>Native <code>se_ar_plan_set</code> (+ <code>se_ar_recompute</code>). Since 0.24; winner and casualties applied since 0.26.2, duels since 0.27.</p>"
        },
        {
          "level": 4,
          "id": "se-modify-autoresolve-plan-clear-ok-message",
          "title": "<code>se.modify.autoresolve_plan_clear() -&gt; ok, message</code>",
          "text": "se.modify.autoresolve_plan_clear() -> ok, message",
          "html": "<p>Drop the stored plan (vanilla behaviour again). No model queue, no multiplayer check.</p>"
        },
        {
          "level": 4,
          "id": "se-query-autoresolve-plan-plan-encoded",
          "title": "<code>se.query.autoresolve_plan() -&gt; plan, encoded</code>",
          "text": "se.query.autoresolve_plan() -> plan, encoded",
          "html": "<p>The plan table as it was given, plus the encoded string the DLL currently holds.</p>"
        },
        {
          "level": 4,
          "id": "se-autoresolve-set-handler-fn-ok-message",
          "title": "<code>se.autoresolve.set_handler(fn) -&gt; ok, message</code>",
          "text": "se.autoresolve.set_handler(fn) -> ok, message",
          "html": "<p>Install a handler called on every <code>PendingBattle</code> that involves <strong>a human player</strong> — on every machine of a multiplayer game, not only the one whose battle it is. <code>fn(ctx)</code> receives the <code>se.query.pending_battle()</code> table and returns a plan table, or <code>nil</code> for vanilla behaviour. Needs <code>se.core</code>. The listener clears any previous plan first, and a second listener drops the plan again on <code>BattleCompleted</code>. Calling it again replaces the handler (<code>\"handler replaced\"</code>).</p><p><strong>Never branch on <code>ctx.*.is_local_player</code></strong> (or <code>cm:get_local_faction()</code>) inside the handler: it is true on one machine and false on the other, so the two would store different plans and desync. Branch on <code>is_human</code> and faction keys, and seed any chance from <code>ctx.seed</code>.</p>"
        },
        {
          "level": 4,
          "id": "se-autoresolve-clear-handler-ok-message",
          "title": "<code>se.autoresolve.clear_handler() -&gt; ok, message</code>",
          "text": "se.autoresolve.clear_handler() -> ok, message",
          "html": "<p>Forget the handler and clear the stored plan. The listeners stay registered but do nothing.</p><hr>"
        },
        {
          "level": 3,
          "id": "3-15-performance-counters-and-the-profiler",
          "title": "3.15 Performance counters and the profiler",
          "text": "3.15 Performance counters and the profiler",
          "html": "<p>Read-only measurement of the DLL's own caches and of the game process. Nothing here changes what the game computes, nothing needs the model thread, and everything is safe in multiplayer. What the caches themselves do, and the <code>script_extender.cfg</code> keys that switch them, is §4b.</p>"
        },
        {
          "level": 4,
          "id": "se-query-perf-table-nil-message",
          "title": "<code>se.query.perf() -&gt; table | nil, message</code>",
          "text": "se.query.perf() -> table | nil, message",
          "html": "<p>The DLL's counters, parsed from the native's <code>k=v;</code> string into numbers (plus <code>installed</code>, a boolean). The set of keys grows with the DLL; the ones worth reading:</p><div class=\"doc-table\"><table><thead><tr><th>Key</th><th>Meaning</th></tr></thead><tbody><tr><td><code>installed</code></td><td>the UI recruit-list cache detour is in place</td></tr><tr><td><code>ttl_ms</code></td><td>its time-to-live, from <code>ui_recruit_cache_ms</code></td></tr><tr><td><code>perm_mode</code>, <code>ai_mode</code></td><td>the <code>recruit_perm_cache</code> / <code>ai_recruit_cache</code> setting actually in effect</td></tr><tr><td><code>hits</code> / <code>misses</code></td><td>UI recruit-list queries answered from the cache / computed by the engine</td></tr><tr><td><code>passed_through</code></td><td>queries from the AI and other non-UI callers, which are never cached</td></tr><tr><td><code>entries</code>, <code>miss_new</code>, <code>miss_expired</code>, <code>miss_state</code>, <code>stamp_failed</code></td><td>why a query missed</td></tr><tr><td><code>perm_built</code>, <code>perm_shared</code>, <code>perm_same</code>, <code>perm_diff</code></td><td>unit-permission tables built, reused, and the result of the self-check (<code>recruit_perm_cache</code>)</td></tr><tr><td><code>ai_*</code></td><td>the diagnostic AI-planner list cache (<code>ai_recruit_cache</code>)</td></tr><tr><td><code>file_probes</code>, <code>file_probes_skipped</code></td><td>loose-file lookups seen and answered without a system call (<code>file_probe_cache_ms</code>)</td></tr><tr><td><code>dip_*</code></td><td>diplomacy evaluation counters, only when <code>diag_diplomacy=1</code></td></tr></tbody></table></div><p>Native <code>se_perf_stats</code>. Since 0.32 (<code>perm_*</code> 0.34, <code>file_probes*</code> 0.35). Status: verified live.</p><pre><code class=\"lang-lua\">local p = se.query.perf()\nif p then ModLog((\"recruit list: %d hits / %d misses, %d AI\"):format(p.hits, p.misses, p.passed_through)) end</code></pre>"
        },
        {
          "level": 4,
          "id": "se-profile-start-seconds-delay-seconds-label-ok-message",
          "title": "<code>se.profile.start(seconds [, delay_seconds [, label]]) -&gt; ok, message</code>",
          "text": "se.profile.start(seconds [, delay_seconds [, label]]) -> ok, message",
          "html": "<p>Start the sampling profiler: every 1 ms it walks the six busiest threads, for <code>seconds</code> (1..120, default 20) after a delay of <code>delay_seconds</code> (default 3, so you can close the console first). <code>label</code> (default <code>\"run\"</code>) names the report. One run at a time; it costs a few percent of frame time while it runs and reads only.</p><p>Two files are written when the run ends, into <code>profiles\\</code> <strong>next to the DLL's version folder</strong> (that is <code>dll\\profiles\\</code> in the mod-manager layout, so the manager's cleanup of old version folders does not take them):</p><ul><li><code>profile_&lt;label&gt;.txt</code> — per thread, the functions the CPU was in (<code>self</code>) and the functions on the stack, as Ghidra addresses;</li><li><code>profile_&lt;label&gt;.folded.txt</code> — folded call stacks, the input for <code>tools/profile_tree.py &lt;folded.txt&gt; [--min 2] [--depth 14] [--callers &lt;fn&gt;] [--minus &lt;baseline&gt;]</code>.</li></ul><p>Native <code>se_profile_start</code>. Since 0.31 (report folder 0.32.2).</p>"
        },
        {
          "level": 4,
          "id": "se-profile-stop-ok-message",
          "title": "<code>se.profile.stop() -&gt; ok, message</code>",
          "text": "se.profile.stop() -> ok, message",
          "html": "<p>End the running profile now; its reports are still written.</p><pre><code class=\"lang-lua\">se.profile.start(50, 4, \"endturn\")   -- close the console, then press End Turn</code></pre><hr>"
        },
        {
          "level": 3,
          "id": "3-16-script-listener-diagnostics",
          "title": "3.16 Script listener diagnostics",
          "text": "3.16 Script listener diagnostics",
          "html": "<p>Which script listener costs the time. This wraps the event manager, not the engine: behaviour is unchanged (same arguments, same return values, errors still pass through the game's own <code>pcall</code>). Read-only, not saved, and it lasts until the Lua state is rebuilt — install it again after a load.</p>"
        },
        {
          "level": 4,
          "id": "se-diag-listeners-start-ok-message",
          "title": "<code>se.diag.listeners_start() -&gt; ok, message</code>",
          "text": "se.diag.listeners_start() -> ok, message",
          "html": "<p>Put a stopwatch around the condition and the callback of every listener registered through <code>core:add_listener</code>, including the ones registered later (<code>core.add_listener</code> itself is wrapped). Needs <code>se.core</code> and the natives <code>se_timer_push</code> / <code>se_timer_pop</code>. Idempotent: a listener is never wrapped twice. Returns <code>\"timing N listeners (new ones are added as they register)\"</code>.</p>"
        },
        {
          "level": 4,
          "id": "se-diag-listeners-reset-true",
          "title": "<code>se.diag.listeners_reset() -&gt; true</code>",
          "text": "se.diag.listeners_reset() -> true",
          "html": "<p>Zero the counters — call it right before the thing you want to measure, e.g. End Turn.</p>"
        },
        {
          "level": 4,
          "id": "se-diag-listeners-report-top-rows",
          "title": "<code>se.diag.listeners_report([top]) -&gt; rows</code>",
          "text": "se.diag.listeners_report([top]) -> rows",
          "html": "<p>Log a per-event summary and the <code>top</code> (default 30) most expensive listeners, and return the rows: <code>{ event, name, calls, fired, total_ms, condition_ms, callback_ms }</code>, sorted by total time. <code>calls</code> counts every evaluation of the condition, <code>fired</code> the ones whose callback ran.</p><p><strong>Times are inclusive</strong>: a callback that triggers further events carries their listeners' time too, so the numbers do not add up to a total. Natives <code>se_timer_push</code> / <code>se_timer_pop</code>. Since 0.35.</p><pre><code class=\"lang-lua\">se.core = core\nse.diag.listeners_start()\nse.diag.listeners_reset()            -- then press End Turn\nse.diag.listeners_report(40)</code></pre><hr>"
        },
        {
          "level": 3,
          "id": "3-17-ai-recruitment",
          "title": "3.17 AI recruitment",
          "text": "3.17 AI recruitment",
          "html": "<p>Two layers: a <strong>read side</strong> (0.36) that shows what the campaign AI budgets, buys and could have bought, and a <strong>policy</strong> (0.37) that adds recruitment orders of its own. §4c and §4d are the narrative versions; this is the reference.</p><p>Engine background: the AI's recruitment planner only ever prices <strong>empty</strong> retinue slots, at most three per character per pass, cheapest first — nothing in the engine replaces a unit that is already in a slot. Its ranking of units is the database table <code>cdir_military_generator_unit_qualities</code>.</p>"
        },
        {
          "level": 4,
          "id": "se-query-unit-quality-unit-key-rows-best",
          "title": "<code>se.query.unit_quality(unit_key) -&gt; rows, best</code>",
          "text": "se.query.unit_quality(unit_key) -> rows, best",
          "html": "<p>The AI's own ranking of a unit, read from the live table (so other mods' rows count): <code>rows = { { group, quality, quality_at_max_xp }, ... }</code>, one row per role group the unit belongs to; <code>best</code> is the highest quality over the groups, or the value in <code>se.ai_recruit.quality_override</code>. An unknown unit gives <code>{}, 0</code>.</p><p>Results are <strong>cached per unit key for the life of the Lua state</strong>; an override set afterwards still wins, because it is applied after the lookup. Native <code>se_unit_quality</code>. Since 0.36.</p>"
        },
        {
          "level": 4,
          "id": "se-ai-recruit-trace-on-was-on",
          "title": "<code>se.ai_recruit.trace(on) -&gt; was_on</code>",
          "text": "se.ai_recruit.trace(on) -> was_on",
          "html": "<p>Let the DLL copy the AI's recruitment requests after every planning pass. <strong>Off by default</strong>; switching it off drops whatever was collected. Native <code>se_ai_recruit_trace</code>. Since 0.36.</p>"
        },
        {
          "level": 4,
          "id": "se-query-ai-recruitment-passes",
          "title": "<code>se.query.ai_recruitment() -&gt; passes</code>",
          "text": "se.query.ai_recruitment() -> passes",
          "html": "<p><strong>Drains</strong> what the trace collected since the last call:</p><pre><code>{ { seq, faction_id, pending,\n    requests = { { money, budget2, turn, target,\n                   rows = { { id, cost, cost2, kind } } } } } }</code></pre><p><code>money</code> / <code>budget2</code> are what the planner set aside for that request, <code>rows</code> the purchases it priced, <code>target</code> the class of the request's target object (hex RVA of its vtable) and <code>faction_id</code> the engine id of the planning faction (<code>0</code> when it could not be resolved). Native <code>se_ai_recruit_passes</code>. Since 0.36.</p>"
        },
        {
          "level": 4,
          "id": "se-ai-recruit-report-faction-key-opts-report-nil-message",
          "title": "<code>se.ai_recruit.report(faction_key [, opts]) -&gt; report | nil, message</code>",
          "text": "se.ai_recruit.report(faction_key [, opts]) -> report | nil, message",
          "html": "<p>A read-only audit of every retinue slot of every army of a faction:</p><pre><code>{ faction, treasury, slots, empty, upgradable, affordable,\n  forces = { { cqi, characters = { { cqi, slots = { {\n      index, unit, experience, strength, recruiting,\n      quality, effective, best = { key, quality, cost, turns }, gap, affordable } } } } } } }</code></pre><p><code>effective</code> scales the unit's quality towards <code>quality_at_max_xp</code> by its experience, the way the AI's own table values a veteran; <code>best</code> is the highest-quality unit of a <strong>shared role group</strong> the slot could recruit right now with no lock reason; <code>gap</code> = <code>best.quality / effective</code>, so a gap above 1 means a better unit is available. <code>opts.min_gap</code> (default 1.0) only fills <code>best</code> when the gap reaches it. Since 0.36.</p>"
        },
        {
          "level": 4,
          "id": "se-ai-recruit-plan-faction-key-money-orders-info",
          "title": "<code>se.ai_recruit.plan(faction_key [, money]) -&gt; orders, info</code>",
          "text": "se.ai_recruit.plan(faction_key [, money]) -> orders, info",
          "html": "<p>What the policy would do this turn. <strong>Changes nothing</strong> — the planning half of the pass, exposed so you can inspect or edit it.</p><pre><code>orders = { { op = \"recruit\" | \"replace\", force, character, general, slot,\n             unit, cost, copies, old, score, old_score, gain }, ... }\ninfo   = { faction, treasury, income, candidates, budget, spent [, skipped ] }</code></pre><p>Orders come back sorted deterministically (replacements first, then by gain, then by character and slot — the same order on every machine), already cut to the budget and the per-turn caps. <code>money = { treasury, income }</code> overrides the faction's own numbers for a what-if. Budget = <code>min(treasury - reserve, max(0, income) * income_turns, max_spend)</code>.</p><p>Score of a unit = its AI quality (the old unit's scaled up by its experience) x the general's element weight x <code>duplicate_penalty</code> per copy already in that retinue. Since 0.37.</p>"
        },
        {
          "level": 4,
          "id": "se-ai-recruit-execute-orders-done-failed",
          "title": "<code>se.ai_recruit.execute(orders) -&gt; done, failed</code>",
          "text": "se.ai_recruit.execute(orders) -> done, failed",
          "html": "<p>Carry the orders out at <strong>normal cost</strong>, through <code>se.modify.replace</code> / <code>se.modify.recruit</code>. Returns two counts. Since 0.37.</p>"
        },
        {
          "level": 4,
          "id": "se-ai-recruit-set-policy-fn-true",
          "title": "<code>se.ai_recruit.set_policy(fn) -&gt; true</code>",
          "text": "se.ai_recruit.set_policy(fn) -> true",
          "html": "<p><code>fn(faction_key) -&gt; orders</code> replaces <code>se.ai_recruit.plan</code> as the decision maker of the turn-start pass; it may call <code>plan()</code> and edit the result. <code>nil</code> restores the default. Since 0.37.</p>"
        },
        {
          "level": 4,
          "id": "se-ai-recruit-enable-ok-message",
          "title": "<code>se.ai_recruit.enable() -&gt; ok, message</code>",
          "text": "se.ai_recruit.enable() -> ok, message",
          "html": "<p>Install the turn-start pass: a <code>FactionTurnStart</code> listener that runs for every AI faction that is not dead. Needs <code>se.core</code>. Once per Lua state — <strong>call it again after a load</strong>. Returns <code>\"listening\"</code>, or <code>\"already listening; enabled\"</code> when it was installed before.</p><p>It runs inside the model callback, so it is legal in multiplayer: no local-player branching and no randomness anywhere in the pass. Both machines must run the same rules, i.e. the same values in the tables below — ship them as mod content, not as a console edit on one machine.</p>"
        },
        {
          "level": 4,
          "id": "se-ai-recruit-disable-true",
          "title": "<code>se.ai_recruit.disable() -&gt; true</code>",
          "text": "se.ai_recruit.disable() -> true",
          "html": "<p>The listener stays registered but does nothing.</p>"
        },
        {
          "level": 4,
          "id": "tunables",
          "title": "Tunables",
          "text": "Tunables",
          "html": "<p>Plain tables; edit them before or after <code>enable()</code>.</p><div class=\"doc-table\"><table><thead><tr><th>Table</th><th>Default</th><th>Meaning</th></tr></thead><tbody><tr><td><code>se.ai_recruit.config</code></td><td>see below</td><td>the policy's numbers</td></tr><tr><td><code>se.ai_recruit.element_order[element]</code></td><td>five lists</td><td>units ranked best to worst for a general of that element. An entry is <code>\"element\"</code> or <code>\"element:class\"</code> with class <code>cavalry</code> / <code>infantry</code>; a unit takes the best entry it matches</td></tr><tr><td><code>se.ai_recruit.element_weight</code></td><td><code>{ 1.30, 1.15, 1.00, 0.90, 0.80 }</code></td><td>score multiplier by rank in that list. A longer list is spread evenly between the first and the last value</td></tr><tr><td><code>se.ai_recruit.unit_element[key]</code></td><td><code>{}</code></td><td>element of a unit whose key does not name one</td></tr><tr><td><code>se.ai_recruit.unit_class[key]</code></td><td><code>{}</code></td><td><code>\"cavalry\"</code> / <code>\"infantry\"</code> for units the quality table does not know</td></tr><tr><td><code>se.ai_recruit.quality_override[key]</code></td><td><code>{}</code></td><td>quality for units missing from the table, or a rebalance</td></tr><tr><td><code>se.ai_recruit.max_experience</code></td><td><code>9</code></td><td>the experience level that counts as fully veteran</td></tr></tbody></table></div><div class=\"doc-table\"><table><thead><tr><th><code>config</code> key</th><th>Default</th><th>Meaning</th></tr></thead><tbody><tr><td><code>fill_empty</code> / <code>replace</code></td><td><code>true</code> / <code>true</code></td><td>which of the two jobs run</td></tr><tr><td><code>min_gain</code></td><td><code>1.5</code></td><td>replace only when the new score is at least this times the old</td></tr><tr><td><code>same_role_only</code></td><td><code>false</code></td><td>a replacement must share a role group with the old unit</td></tr><tr><td><code>min_strength</code></td><td><code>50</code></td><td>leave units below this strength % alone</td></tr><tr><td><code>max_per_character</code> / <code>max_per_force</code> / <code>max_per_faction</code></td><td><code>2</code> / <code>3</code> / <code>8</code></td><td>orders per turn</td></tr><tr><td><code>reserve</code></td><td><code>1500</code></td><td>treasury the pass never touches</td></tr><tr><td><code>income_turns</code> / <code>max_spend</code></td><td><code>3</code> / <code>4000</code></td><td>the other two budget limits</td></tr><tr><td><code>min_income</code></td><td><code>0</code></td><td>a faction with a lower projected income does nothing</td></tr><tr><td><code>duplicate_penalty</code></td><td><code>0.92</code></td><td>score multiplier per copy of the unit already in the retinue</td></tr><tr><td><code>max_copies</code></td><td><code>0</code></td><td>hard limit of copies of one unit per retinue (0 = none)</td></tr><tr><td><code>log_orders</code></td><td><code>true</code></td><td>log every executed order with before / after</td></tr></tbody></table></div>"
        },
        {
          "level": 4,
          "id": "helpers",
          "title": "Helpers",
          "text": "Helpers",
          "html": "<p><code>se.ai_recruit.element_of(key) -&gt; element | nil</code> — the first <code>_</code>-separated word of a unit or character-subtype key that names an element (<code>unit_element</code> overrides). <code>se.ai_recruit.class_of(unit_key) -&gt; \"cavalry\" | \"infantry\"</code> — cavalry when any of the unit's role groups names cavalry (<code>unit_class</code> overrides). <code>se.ai_recruit.element_factor(general_element, unit_key) -&gt; number</code> — the weight of the best entry of that general's list the unit matches, 1 when either side is unknown. <code>se.ai_recruit.forces_of(faction_key) -&gt; { { cqi, characters = { { cqi, element } } } }</code> — the pass's data source, kept separate so a test or another script can replace it.</p><hr>"
        },
        {
          "level": 2,
          "id": "4-recipes",
          "title": "4. Recipes",
          "text": "4. Recipes",
          "html": "<p>Each of these is a complete script. Console scripts live in <code>&lt;game root&gt;\\lua_scripts\\</code> and are listed in <code>index.txt</code>; the same code works from a mod script (<code>script/campaign/mod/...</code>), where <code>cm:callback</code> also works.</p>"
        },
        {
          "level": 3,
          "id": "4-1-move-a-character-into-another-faction-s-pool-and-unlock-them",
          "title": "4.1 Move a character into another faction's pool and unlock them",
          "text": "4.1 Move a character into another faction's pool and unlock them",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_move.lua : cqi CQI -&gt; TARGET's recruitment pool, available immediately.\nlocal CQI    = 72\nlocal TARGET = \"3k_main_faction_cao_cao\"\n\nlocal function log(s) ModLog(\"[recipe_move] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" then log(\"script extender not present\") return end\nse.logger = ModLog\n\nlog(\"before: \" .. se.dump(se.query.character(CQI) or {}))\nlocal ok, msg = se.modify.move_character(CQI, TARGET, \"pool\")\nlog(\"move_character -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg))\n\n-- The move's own state step runs 1 s later via cm:callback (which does not fire from the\n-- console), so unlock on the model queue instead; the queue runs in order.\ncm:wait_for_model_sp(function()\n    local c = se.query.character(CQI)\n    if c and c.in_pool then\n        local ok2, msg2 = se.modify.pool_unlock(CQI)\n        log(\"pool_unlock -&gt; \" .. tostring(ok2) .. \" : \" .. tostring(msg2))\n    else\n        log(\"not in a pool yet: \" .. se.dump(c or {}))\n    end\n    log(\"lock now: \" .. tostring(se.query.pool_lock(CQI)))\nend)\n-- reopen the court screen to see the candidate list refresh</code></pre>"
        },
        {
          "level": 3,
          "id": "4-2-recruit-a-locked-unit-into-an-empty-retinue-slot-at-50-strength",
          "title": "4.2 Recruit a locked unit into an empty retinue slot at 50% strength",
          "text": "4.2 Recruit a locked unit into an empty retinue slot at 50% strength",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_recruit.lua\nlocal CQI      = 1\nlocal UNIT_KEY = \"3k_dlc04_unit_wood_imperial_gate_guards\"\n\nlocal function log(s) ModLog(\"[recipe_recruit] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" or not se.available(\"se_recruit_unit\") then log(\"SE missing\") return end\nse.logger = ModLog\n\n-- find the first empty slot\nlocal slot\nfor _, row in ipairs(se.query.retinue(CQI) or {}) do\n    if not slot and row.index &gt; 0 and row.unit_key == \"\" and row.can_recruit then slot = row.index end\nend\nif not slot then log(\"no empty slot\") return end\n\nlocal ok, msg = se.modify.recruit(CQI, UNIT_KEY,\n    { source = \"any\", free = true, slot = slot })\nlog(\"recruit -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg))\n\n-- opts.hp would be applied through cm:callback, which does not fire from the console:\ncm:wait_for_model_sp(function()\n    local ok2, msg2 = se.modify.unit_strength(CQI, slot, 50)\n    log(\"unit_strength -&gt; \" .. tostring(ok2) .. \" : \" .. tostring(msg2))\n    log(\"slot now: \" .. se.dump(se.query.unit(CQI, slot) or {}))\nend)</code></pre>"
        },
        {
          "level": 3,
          "id": "4-3-force-the-three-kingdoms-with-a-ban-list",
          "title": "4.3 Force the Three Kingdoms with a ban list",
          "text": "4.3 Force the Three Kingdoms with a ban list",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_three_kingdoms.lua\nlocal FORCED = { \"3k_main_faction_kong_rong\" }\nlocal BANNED = { \"3k_main_faction_dong_zhuo\", \"3k_main_faction_yuan_shu\" }\n\nlocal function log(s) ModLog(\"[recipe_3k] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" then log(\"SE missing\") return end\nse.logger = ModLog\nse.core = core\n\nlog(\"leaders before: \" .. table.concat(se.query.world_leaders() or {}, \", \"))\nlocal ok, msg = se.modify.force_three_kingdoms{ forced = FORCED, banned = BANNED }\nlog(\"force_three_kingdoms -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg))\ncm:wait_for_model_sp(function()\n    log(\"leaders after: \" .. table.concat(se.query.world_leaders() or {}, \", \"))\nend)\n-- keep the bans in force for the rest of the campaign (saved; call se.load_emperor_policy()\n-- after every load)\nse.modify.emperor_policy{ forced = FORCED, banned = BANNED }</code></pre>"
        },
        {
          "level": 3,
          "id": "4-4-force-build-a-free-building-in-an-empty-slot",
          "title": "4.4 Force-build a free building in an empty slot",
          "text": "4.4 Force-build a free building in an empty slot",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_build.lua\nlocal FACTION = \"3k_main_faction_cao_cao\"\n\nlocal function log(s) ModLog(\"[recipe_build] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" or not se.available(\"se_slot_construct\") then log(\"SE too old\") return end\nse.logger = ModLog\n\nlocal okr, region = pcall(function() return cm:query_faction(FACTION):capital_region():name() end)\nif not okr then log(\"no capital region\") return end\n\nlocal empty\nfor _, row in ipairs(se.query.region_slots(region) or {}) do\n    if not row.has_building and empty == nil then empty = row.index end\nend\nif not empty then log(\"no empty slot in \" .. region) return end\n\nlocal candidates = se.query.building_candidates(region, empty, { all_chains = true })\nlocal level = candidates[1]\nif not level then log(\"no candidates for slot \" .. empty) return end\n\nlocal ok, msg = se.modify.building_construct(region, empty, level,\n    { force = true, any_chain = true, free = true, complete = true })\nlog(\"construct \" .. level .. \" -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg))\n-- It completes at the NEXT turn start, and only if the province is under its construction limit.</code></pre>"
        },
        {
          "level": 3,
          "id": "4-5-rename-a-coalition",
          "title": "4.5 Rename a coalition",
          "text": "4.5 Rename a coalition",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_rename_alliance.lua\nlocal MEMBER = \"3k_main_faction_yuan_shao\"\nlocal TEXT   = \"Yuan Shao's Grand Coalition\"\n\nlocal function log(s) ModLog(\"[recipe_alliance] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" or not se.available(\"se_alliance_name_set\") then log(\"SE too old\") return end\nse.logger = ModLog\n\nlocal target\nfor _, a in ipairs(se.query.alliances() or {}) do\n    log(\"cqi \" .. tostring(a.cqi) .. \" '\" .. tostring(a.name) .. \"': \" .. table.concat(a.members, \", \"))\n    for _, m in ipairs(a.members) do if m == MEMBER then target = a.cqi end end\nend\nif not target then log(\"no alliance contains \" .. MEMBER) return end\nlocal ok, msg = se.modify.alliance_name(target, TEXT, \"inline\")\nlog(\"alliance_name -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg))\n-- run the script again to see the new name; verify it survives a save/load before shipping</code></pre>"
        },
        {
          "level": 3,
          "id": "4-6-redefine-an-effect-bundle-and-apply-it-re-applied-after-every-load",
          "title": "4.6 Redefine an effect bundle and apply it (re-applied after every load)",
          "text": "4.6 Redefine an effect bundle and apply it (re-applied after every load)",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_bundle.lua : put this in a mod script so the re-define also runs after a load.\nlocal FACTION = \"3k_main_faction_cao_cao\"\nlocal BUNDLE  = \"3k_dlc05_effect_bundle_ai_cao_cao_bonus_income\"\nlocal EFFECTS = {\n    { effect = \"3k_main_effect_characters_experience_bonus\",\n      scope  = \"faction_to_character_own_factionwide_unseen\", value = 25 },\n}\n\nlocal function log(s) ModLog(\"[recipe_bundle] \" .. tostring(s)) end\nif type(se) ~= \"table\" or not se.available(\"se_effect_bundle_define\") then log(\"SE too old\") return end\nse.logger = ModLog\nse.core = core\n\nlocal function install()\n    local ok, msg = se.modify.effect_bundle_define(BUNDLE, EFFECTS, FACTION)\n    log(\"define -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg))\n    cm:wait_for_model_sp(function()\n        -- a holder that already carries the bundle keeps the OLD effect list until the bundle is\n        -- removed and applied again, so apply it here after every define\n        local ok2, e = pcall(function() cm:modify_faction(FACTION):apply_effect_bundle(BUNDLE, 0) end)\n        log(\"stock apply -&gt; \" .. tostring(ok2) .. \" \" .. tostring(e))\n    end)\nend\n\ninstall()                      -- the definition is NOT saved:\ncore:add_listener(\"recipe_bundle_load\", \"LoadingGame\", true, function() install() end, true)</code></pre>"
        },
        {
          "level": 3,
          "id": "4-7-cap-the-player-s-auto-resolve-casualties-and-force-a-loss-against-one-faction",
          "title": "4.7 Cap the player's auto-resolve casualties and force a loss against one faction",
          "text": "4.7 Cap the player's auto-resolve casualties and force a loss against one faction",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_ar_rules.lua : load once per session (or ship the same code in a mod script).\nlocal MINE     = \"3k_main_faction_cao_cao\"     -- the faction the rules protect, by key: the same\n                                               -- on every machine (never is_local_player)\nlocal NEMESIS  = \"3k_main_faction_dong_zhuo\"   -- battles against this faction are always lost\nlocal MY_CAP   = 0.25                          -- that faction never loses more than 25% per unit\n\nlocal function log(s) ModLog(\"[recipe_ar] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" or type(se.autoresolve) ~= \"table\" then log(\"SE too old\") return end\nse.logger = ModLog\nse.core = core\n\nlocal function handler(ctx)\n    local attacking = ctx.attacker.faction == MINE\n    if not (attacking or ctx.defender.faction == MINE) then return nil end   -- not our battle\n    local mine     = attacking and \"attacker\" or \"defender\"\n    local theirs   = attacking and \"defender\" or \"attacker\"\n    local enemy    = ctx[theirs]\n    log(\"battle vs \" .. tostring(enemy.faction) .. \" (\" .. tostring(ctx.battle_type) .. \")\")\n\n    local plan = { casualties = { [mine] = { max = MY_CAP } } }\n    if enemy.faction == NEMESIS then\n        plan.winner = theirs                       -- forced defeat\n        plan.casualties[mine] = { max = 1.0 }      -- and take the losses that come with it\n    end\n    return plan\nend\n\nlocal ok, msg = se.autoresolve.set_handler(handler)\nlog(\"set_handler -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg))\n-- The handler runs on every machine, so it must decide from faction keys only. plan.duels works\n-- since 0.27; plan.bias is stored but never applied.</code></pre>"
        },
        {
          "level": 3,
          "id": "4-8-a-per-turn-income-line-that-survives-loading",
          "title": "4.8 A per-turn income line that survives loading",
          "text": "4.8 A per-turn income line that survives loading",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_income.lua\nlocal FACTION = \"3k_main_faction_cao_cao\"\n\nlocal function log(s) ModLog(\"[recipe_income] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" then log(\"SE missing\") return end\nse.logger = ModLog\nse.core = core\n\nlocal ok, msg = se.modify.faction_income(FACTION, 1500, \"horde_income\")\nlog(\"faction_income -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg))\nlog(\"lines: \" .. se.dump(se.query.faction_income(FACTION)))\n\n-- after a load, restore the saved lines and re-install the listener:\ncore:add_listener(\"recipe_income_load\", \"LoadingGame\", true, function()\n    se.core = core\n    log(\"load_income_lines -&gt; \" .. tostring(se.load_income_lines()))\nend, true)\n-- the amount is paid at FactionTurnStart; it does not show in the income breakdown.</code></pre>"
        },
        {
          "level": 3,
          "id": "4-9-re-tune-an-ai-faction-personality-potential-attitude",
          "title": "4.9 Re-tune an AI faction: personality, potential, attitude",
          "text": "4.9 Re-tune an AI faction: personality, potential, attitude",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_ai.lua\nlocal FACTION     = \"3k_main_faction_dong_zhuo\"\nlocal PERSONALITY = \"3k_cai_personality_dong_zhuo_late_hard\"\nlocal POTENTIAL   = 140                          -- -100..150\n\nlocal function log(s) ModLog(\"[recipe_ai] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" then log(\"SE missing\") return end\nse.logger = ModLog\n\nlocal function report(what, ok, msg) log(what .. \" -&gt; \" .. tostring(ok) .. \" : \" .. tostring(msg)) end\n\nlog(\"potential before: \" .. se.dump(se.query.faction_potential(FACTION) or {}))\n-- cai_personality needs the model thread, so read it there\nse.on_model(\"read personality\", function()\n    log(\"personality before: \" .. se.dump(se.query.cai_personality(FACTION) or {}))\n    return true, \"read\"\nend)\n\nreport(\"cai_personality\", se.modify.cai_personality(FACTION, PERSONALITY))\nreport(\"faction_potential\", se.modify.faction_potential(FACTION, POTENTIAL))\nreport(\"attitude\", se.modify.attitude(\"3k_main_faction_cao_cao\", FACTION, -3))\n-- personality and potential persist across save/load; the attitude event is DB-valued.</code></pre>"
        },
        {
          "level": 3,
          "id": "4-10-grant-experience-and-unspent-skill-points-to-a-character",
          "title": "4.10 Grant experience and unspent skill points to a character",
          "text": "4.10 Grant experience and unspent skill points to a character",
          "html": "<pre><code class=\"lang-lua\">-- se_recipe_xp.lua\nlocal CQI = 14\n\nlocal function log(s) ModLog(\"[recipe_xp] \" .. tostring(s)) end\npcall(function() return cm:query_model():world() end)\nif type(se) ~= \"table\" or not se.available(\"se_char_xp_add\") then log(\"SE too old\") return end\nse.logger = ModLog\n\nlog(\"before: \" .. se.dump(se.query.character_xp(CQI) or {}))\nlog(\"add_xp -&gt; \" .. tostring(select(2, se.modify.character_add_xp(CQI, 5000, false))))  -- exact\nlog(\"skill_points -&gt; \" .. tostring(select(2, se.modify.skill_points(CQI, 5))))\ncm:wait_for_model_sp(function()\n    log(\"after: \" .. se.dump(se.query.character_xp(CQI) or {}))\nend)</code></pre><hr>"
        },
        {
          "level": 2,
          "id": "4b-script-extender-cfg-and-the-performance-features-dll-0-31-0-35",
          "title": "4b. <code>script_extender.cfg</code> and the performance features (DLL 0.31 - 0.35)",
          "text": "4b. script_extender.cfg and the performance features (DLL 0.31 - 0.35)",
          "html": ""
        },
        {
          "level": 3,
          "id": "the-configuration-file",
          "title": "The configuration file",
          "text": "The configuration file",
          "html": "<p>The DLL reads <code>script_extender.cfg</code> <strong>next to itself, then in its parent folder</strong> — the second place is the mod-manager layout, where the DLL sits in <code>dll\\&lt;version&gt;\\</code> and the file in <code>dll\\</code>, so one file serves every installed version. Format: one <code>key=value</code> per line, <code>#</code> comments, values optionally quoted; an unknown key is logged and ignored.</p><div class=\"doc-table\"><table><thead><tr><th>Key</th><th>Default</th><th>Purpose</th></tr></thead><tbody><tr><td><code>build_number</code>, <code>build_number_short</code>, <code>build_modified</code></td><td>—</td><td>menu build strings applied at injection (§3.13)</td></tr><tr><td><code>autoresolve_hooks</code></td><td>on</td><td><code>0</code> disables every auto-resolve hook (§3.14)</td></tr><tr><td><code>horde_income</code></td><td>on</td><td><code>0</code> disables the horde income hook (§3.12a)</td></tr><tr><td><code>horde_income_category</code></td><td><code>1</code> (MINING)</td><td>which income category receives the total, <code>0..3</code></td></tr><tr><td><code>recruit_perm_cache</code></td><td><code>1</code></td><td>unit-permission table reuse, see below</td></tr><tr><td><code>ui_recruit_cache_ms</code></td><td><code>5000</code> (max 30000)</td><td>UI recruit-list cache TTL</td></tr><tr><td><code>ai_recruit_cache</code></td><td><code>0</code></td><td>diagnostic AI-planner list cache</td></tr><tr><td><code>file_probe_cache_ms</code></td><td><code>10000</code> (max 600000)</td><td>missing-directory cache for loose-file lookups</td></tr><tr><td><code>diag_diplomacy</code></td><td><code>0</code></td><td>diplomacy evaluation counters only</td></tr></tbody></table></div><p><strong>Five of them are part of the multiplayer version lock</strong> — <code>autoresolve_hooks</code>, <code>ai_recruit_cache</code>, <code>recruit_perm_cache</code>, <code>horde_income</code> and <code>horde_income_category</code>. The DLL hashes their <em>effective</em> values into the build string, so an absent key and an explicitly written default give the same tag, but two players with different values cannot join each other (§1).</p>"
        },
        {
          "level": 3,
          "id": "the-performance-features",
          "title": "The performance features",
          "text": "The performance features",
          "html": "<p>These need no script. None of them changes what the game computes.</p><div class=\"doc-table\"><table><thead><tr><th>cfg key</th><th>default</th><th>what it does</th></tr></thead><tbody><tr><td><code>recruit_perm_cache</code></td><td><code>1</code></td><td>The engine's \"what can this retinue slot recruit\" routine rebuilt a complete table of the faction's unit permissions <strong>for every candidate unit</strong> (units x permissions per list; with a large unit roster this was the biggest single cost of an AI turn and of an open character panel). The DLL lets the engine build each table once per list and reuses it. The first 3000 reuses of a session are checked against the engine's own rebuild; one difference turns the feature off for the session (DLL log). <code>0</code> = off, <code>2</code> = check only.</td></tr><tr><td><code>ui_recruit_cache_ms</code></td><td><code>5000</code></td><td>UI only: a recruitable-unit list asked for again by the panels is served from memory while the faction's treasury and the turn are unchanged, for at most this long. <code>0</code> = off.</td></tr><tr><td><code>file_probe_cache_ms</code></td><td><code>10000</code></td><td>Before reading a file from a pack the engine looks for a loose copy in every search root (each subscribed mod folder, <code>data</code>, ...) and remembers nothing: zooming the camera in fired 1316 failed lookups in one burst. The DLL remembers for this long that a <em>directory</em> does not exist and answers lookups into it without a system call. A folder created while the game runs is seen after at most this time. <code>0</code> = off.</td></tr><tr><td><code>diag_diplomacy</code></td><td><code>0</code></td><td>Measurement only (<code>dip_*</code> counters).</td></tr><tr><td><code>ai_recruit_cache</code></td><td><code>0</code></td><td>Diagnostic (whole-list cache inside the AI's recruitment budget planner). Measured as not worth it; leave off.</td></tr></tbody></table></div><p>Measured on a modded late campaign: end turn 76 s -&gt; 45 s (horde income hook made cheap in 0.32.4) -&gt; about 30 s (permission tables, 0.34); turn 1 went 62 s -&gt; 39 s.</p>"
        },
        {
          "level": 3,
          "id": "reading-the-numbers-from-a-script",
          "title": "Reading the numbers from a script",
          "text": "Reading the numbers from a script",
          "html": "<p>The full reference for these three groups is §3.15 and §3.16.</p><pre><code class=\"lang-lua\">se.query.perf()                     -- cache counters: hits / misses / passed_through, perm_*,\n                                    -- ai_*, file_probes*\nse.profile.start(50, 4, \"endturn\")  -- CPU sampling: 50 s after a 4 s delay; writes\n                                    -- profile_endturn.txt + .folded.txt into dll\\profiles\\\nse.profile.stop()                   -- end the run now; the reports are still written\n\nse.core = core                      -- hand over the event manager once\nse.diag.listeners_start()           -- time every core:add_listener listener from now on\nse.diag.listeners_reset()           -- zero the numbers (e.g. right before End Turn)\nse.diag.listeners_report(40)        -- log totals per event and the 40 most expensive listeners</code></pre><p><code>tools/profile_tree.py &lt;folded.txt&gt; [--min 2] [--depth 14] [--callers &lt;fn&gt;] [--minus &lt;baseline&gt;]</code> turns the folded stacks into a call tree (addresses are Ghidra function starts).</p><hr>"
        },
        {
          "level": 2,
          "id": "4c-watching-the-campaign-ai-recruit-dll-0-36-read-only-reference-in-3-17",
          "title": "4c. Watching the campaign AI recruit (DLL 0.36+, read-only; reference in §3.17)",
          "text": "4c. Watching the campaign AI recruit (DLL 0.36+, read-only; reference in §3.17)",
          "html": "<p>The AI's recruitment planner only ever prices <strong>empty</strong> retinue slots (at most three per character per pass, cheapest first); nothing in the engine replaces a unit that is already in a slot. These calls show what it budgets, what it buys and what each slot could have had.</p><pre><code class=\"lang-lua\">se.ai_recruit.trace(true)                 -- let the DLL copy the planner's requests (off by default)\nlocal passes = se.query.ai_recruitment()  -- drains them: { { seq, faction_id, pending, requests = {\n                                          --   { money, budget2, turn, target, rows = { { id, cost, cost2, kind } } } } } }\nlocal rows, best = se.query.unit_quality(\"3k_main_unit_metal_jian_sword_guards\")\n                                          -- the AI's own ranking (cdir_military_generator_unit_qualities,\n                                          -- read live): { { group, quality, quality_at_max_xp } }, best\nse.ai_recruit.quality_override[\"my_mod_unit\"] = 2400   -- units the table does not know, or a rebalance\nlocal r = se.ai_recruit.report(\"3k_main_faction_cao_cao\", { min_gap = 1.25 })\n-- r = { faction, treasury, slots, empty, upgradable, affordable, forces = { { cqi, characters = { {\n--   cqi, slots = { { index, unit, experience, strength, recruiting, quality, effective,\n--   best = { key, quality, cost, turns }, gap, affordable } } } } } } }</code></pre><p><code>effective</code> scales a unit's quality towards <code>quality_at_max_xp</code> with its experience, the way the AI's own table values a veteran; <code>gap</code> = best option / effective. Stock event <code>UnitRecruitmentInitiated</code> (<code>context:faction()</code>, <code>context:unit_key()</code>) tells what was really bought. The console script <code>se_ai_trace.lua</code> logs all three per AI faction turn.</p><hr>"
        },
        {
          "level": 2,
          "id": "4d-ai-recruitment-policy-better-armies-that-fit-their-general-dll-0-37-reference-in-3-17",
          "title": "4d. AI recruitment policy: better armies that fit their general (DLL 0.37+; reference in §3.17)",
          "text": "4d. AI recruitment policy: better armies that fit their general (DLL 0.37+; reference in §3.17)",
          "html": "<p>The engine fills empty retinue slots with the cheapest unit and never replaces a unit. This pass runs at the start of every AI faction's turn, inside the model callback (the same on every multiplayer machine), at normal recruitment cost:</p><pre><code class=\"lang-lua\">se.core = core\nse.ai_recruit.element_order.wood = { \"wood\", \"metal\", \"water\", \"earth\", \"fire\" }  -- best first, per general element\nse.ai_recruit.element_order.fire = { \"fire\", \"earth\", \"water:cavalry\", \"metal\", \"wood\", \"water\" }  -- \"element:cavalry\" / \"element:infantry\" entries: a unit takes the best entry it matches\nse.ai_recruit.unit_class[\"my_unit\"] = \"cavalry\"       -- class of units the quality table does not know (cavalry = a role group naming cavalry)\nse.ai_recruit.element_weight = { 1.30, 1.15, 1.00, 0.90, 0.80 }                  -- multiplier by rank in that list\nse.ai_recruit.unit_element[\"my_unit\"] = \"metal\"       -- keys that do not name their element\nse.ai_recruit.quality_override[\"my_unit\"] = 2400      -- units the game's quality table lacks\nse.ai_recruit.config.min_gain = 1.5                   -- see the table below\nse.ai_recruit.enable()                                -- once per Lua state (again after a load)\nse.ai_recruit.disable()\n\nlocal orders, info = se.ai_recruit.plan(\"3k_main_faction_cao_cao\")   -- what it would do; changes nothing\nse.ai_recruit.execute(orders)                                        -- carry orders out\nse.ai_recruit.set_policy(function(faction_key)                       -- your own decision maker\n\tlocal orders, info = se.ai_recruit.plan(faction_key)\n\t-- edit / filter / replace the orders: { op = \"recruit\" | \"replace\", character, slot, unit, cost, ... }\n\treturn orders, info\nend)</code></pre><p>score = the game's AI quality of the unit (<code>se.query.unit_quality</code>, the old unit's scaled up with its experience) x element weight (the general's element comes from <code>character_subtype_key()</code>, or, for captains and other leaders whose subtype names none, from their bodyguard unit in slot 0; the unit's element from its key: the first <code>_</code>-separated word that names one) x <code>duplicate_penalty</code> per copy already in the retinue. Empty slots take the best score; an occupied slot is replaced when the best score is at least <code>min_gain</code> times the old one.</p><p>Every <code>config</code> key with its default is in §3.17; the ones you are most likely to move are <code>min_gain</code> (how much better a replacement must be), <code>reserve</code> / <code>income_turns</code> / <code>max_spend</code> (the budget) and <code>max_per_character</code> / <code>max_per_force</code> / <code>max_per_faction</code> (how fast an army may change).</p><p>Console script with all of this at the top: <code>se_ai_recruit_rules.lua</code>. Test: <code>tools/test_lua_aipolicy.py</code>.</p><hr>"
        },
        {
          "level": 2,
          "id": "5-troubleshooting",
          "title": "5. Troubleshooting",
          "text": "5. Troubleshooting",
          "html": ""
        },
        {
          "level": 3,
          "id": "the-api-is-not-there-at-all",
          "title": "The API is not there at all",
          "text": "The API is not there at all",
          "html": "<ul><li><code>type(se) ~= \"table\"</code>: the DLL is not injected, or it has not yet registered into <em>this</em> Lua state. Registration happens the first time a state ticks Lua, so the main-menu state is served first and the campaign state once a campaign has loaded.</li><li>The DLL is inert when its fingerprint check fails. Look in its log for <code>fingerprint mismatch: expected ...</code> or <code>N anchor(s) failed; refusing to run</code> — that means the game was updated and the DLL must be rebuilt against the new build. A healthy log contains <code>all N addresses and M vtables verified</code>, <code>lua_gettop hook installed</code>, then per state <code>registered se_* functions into lua_State ...</code> and <code>se_api.lua loaded into lua_State ...</code>.</li><li>Never inject twice into the same process, and never inject a second (or newer) DLL on top of a running one. To update: quit the game, start it, inject once after the main menu is up.</li></ul>"
        },
        {
          "level": 3,
          "id": "common-refusal-messages",
          "title": "Common refusal messages",
          "text": "Common refusal messages",
          "html": "<div class=\"doc-table\"><table><thead><tr><th>Message</th><th>Meaning</th></tr></thead><tbody><tr><td><code>native se_x is not available (DLL too old or not injected)</code></td><td>this DLL predates the feature; gate with <code>se.available</code></td></tr><tr><td><code>refused in multiplayer: ... called outside a model callback</code></td><td>multiplayer is lockstep: call <code>se.modify.*</code> from an event listener / turn-start callback that runs on every machine, not from UI code or the console</td></tr><tr><td><code>campaign manager (cm) is not available in this Lua state</code></td><td>called from a state with no <code>cm</code> (for example the frontend)</td></tr><tr><td><code>core (event manager) is not available: set se.core = core ...</code></td><td>assign <code>se.core = core</code> before <code>emperor_policy</code>, <code>faction_income</code>, <code>autoresolver_variable</code> or <code>set_handler</code></td></tr><tr><td><code>&lt;tag&gt; queued on the model thread (result in the log / callback)</code></td><td>not an error: the real result is in the log</td></tr><tr><td><code>no character with cqi N</code> / <code>cqi mismatch for N</code></td><td>the cqi does not resolve, or the interface reports a different cqi</td></tr><tr><td><code>no faction '&lt;key&gt;'</code> / <code>no region '&lt;key&gt;'</code></td><td>wrong database key</td></tr><tr><td><code>character commands no persistent retinue</code></td><td>the character has no retinue (not a general)</td></tr><tr><td><code>slot is not linked to a military force slot (army not deployed?)</code></td><td>the slot's unit is not on the map, so strength/experience cannot be read or written</td></tr><tr><td><code>slot N holds &lt;key&gt;; pass replace = true to swap it</code></td><td>target an empty slot or pass <code>replace</code></td></tr><tr><td><code>&lt;key&gt; is locked (reasons 0x...); use opts.source = 'locked' or 'any'</code></td><td>the item is not unlocked in that tree</td></tr><tr><td><code>refusing to disband the commander's own slot</code></td><td>slot 0 is the commander</td></tr><tr><td><code>refused: no pending battle with a human player</code></td><td>no pre-battle panel, or the battle is AI against AI</td></tr><tr><td><code>effects must be a non-empty list of {effect=, scope=, value=}</code></td><td>malformed effect bundle list</td></tr><tr><td><code>cm:modify_campaign_ai() is not available (needs the model thread)</code></td><td><code>se.query.cai_personality</code> was called outside a model callback — wrap it in <code>se.on_model</code></td></tr><tr><td><code>state must be 'pool' or 'recruited'</code></td><td>third argument of <code>move_character</code></td></tr><tr><td><code>usage: autoresolver_variable(key, number)</code></td><td>the key must be a string and the value a number</td></tr><tr><td><code>a profile is already running</code></td><td>one profiler run at a time; <code>se.profile.stop()</code> first</td></tr><tr><td><code>no saved policy</code> / <code>no saved income lines</code></td><td><code>se.load_emperor_policy()</code> / <code>se.load_income_lines()</code> found nothing in the save</td></tr></tbody></table></div>"
        },
        {
          "level": 3,
          "id": "nothing-visibly-changed",
          "title": "Nothing visibly changed",
          "text": "Nothing visibly changed",
          "html": "<ul><li><strong>Court candidate list</strong>: close and reopen the court screen after <code>pool_unlock</code> / <code>move_character</code>.</li><li><strong>Retinue / unit panel</strong>: re-read after a moment; some values only refresh when the panel is rebuilt.</li><li><strong>Building</strong>: right after <code>building_construct</code> the slot still reports the old building; the construction completes at the next turn start (and is dropped if the province is over its construction limit).</li><li><strong>Auto-resolve panel</strong>: the casualty bar does not refresh after a plan recompute; the numbers the engine uses are still the planned ones.</li><li><strong>A queued call</strong>: check the log for the real result line, <code>\"&lt;tag&gt; -&gt; true : ...\"</code>.</li></ul>"
        },
        {
          "level": 3,
          "id": "where-the-logs-are",
          "title": "Where the logs are",
          "text": "Where the logs are",
          "html": "<ul><li><strong>DLL log</strong>: <code>script_extender.log</code> next to the injected <code>script_extender.dll</code> (truncated on every injection, also mirrored to <code>OutputDebugString</code>).</li><li><strong>Lua log</strong> (in-game console environment): <code>lua_mod_log.txt</code> in the game root and the per-session <code>ironic_log_&lt;faction&gt;_&lt;stamp&gt;.txt</code> — the newest by modification time is the current one.</li><li><strong>Crash dumps</strong>: <code>%LOCALAPPDATA%\\CrashDumps</code>.</li></ul>"
        },
        {
          "level": 3,
          "id": "reporting-a-crash",
          "title": "Reporting a crash",
          "text": "Reporting a crash",
          "html": "<p>Send: the game build number, the DLL version (<code>se.version()</code>), the <strong>DLL log</strong> next to the DLL, the minidump from <code>%LOCALAPPDATA%\\CrashDumps</code>, the Lua log, and the exact script that was running. Every crash the project has had so far came from an engine routine being handed the wrong object; the natives now validate vtables, back-pointers and registry round-trips and refuse instead of writing, so a refusal message is the expected outcome of a bad argument — a crash is a bug worth reporting.</p><hr>"
        },
        {
          "level": 2,
          "id": "6-index-of-public-functions",
          "title": "6. Index of public functions",
          "text": "6. Index of public functions",
          "html": "<p>Every function the module defines. Optional arguments in <code>[ ]</code>; §3 has the details.</p><div class=\"doc-table\"><table><thead><tr><th>Function</th><th>Description</th></tr></thead><tbody><tr><td><code>se.ai_recruit.class_of(unit_key)</code></td><td>cavalry or infantry, from the unit's role groups</td></tr><tr><td><code>se.ai_recruit.disable()</code></td><td>stop the AI recruitment pass (the listener stays registered)</td></tr><tr><td><code>se.ai_recruit.element_factor(element, unit_key)</code></td><td>score multiplier of a unit for a general of that element</td></tr><tr><td><code>se.ai_recruit.element_of(key)</code></td><td>the element named by a unit or character-subtype key</td></tr><tr><td><code>se.ai_recruit.enable()</code></td><td>install the AI recruitment pass at faction turn start</td></tr><tr><td><code>se.ai_recruit.execute(orders)</code></td><td>carry recruitment orders out at normal cost</td></tr><tr><td><code>se.ai_recruit.forces_of(faction_key)</code></td><td>the pass's own view of a faction's armies</td></tr><tr><td><code>se.ai_recruit.plan(faction_key [, money])</code></td><td>the orders the policy would issue; changes nothing</td></tr><tr><td><code>se.ai_recruit.report(faction_key [, opts])</code></td><td>per-slot audit: unit, quality, best option, gap</td></tr><tr><td><code>se.ai_recruit.set_policy(fn)</code></td><td>replace the pass's decision maker</td></tr><tr><td><code>se.ai_recruit.trace(on)</code></td><td>let the DLL copy the AI's recruitment requests</td></tr><tr><td><code>se.autoresolve.clear_handler()</code></td><td>forget the auto-resolve handler and clear the stored plan</td></tr><tr><td><code>se.autoresolve.set_handler(fn)</code></td><td>call fn(ctx) on every PendingBattle with a human player</td></tr><tr><td><code>se.available(native)</code></td><td>is a given se_* native present in this Lua state</td></tr><tr><td><code>se.character(cqi)</code></td><td>checked cm:query_character</td></tr><tr><td><code>se.diag.listeners_report([top])</code></td><td>totals per event and the most expensive listeners</td></tr><tr><td><code>se.diag.listeners_reset()</code></td><td>zero the listener timings</td></tr><tr><td><code>se.diag.listeners_start()</code></td><td>time every core:add_listener listener from now on</td></tr><tr><td><code>se.dump(t [, indent])</code></td><td>pretty-print a table</td></tr><tr><td><code>se.faction(key)</code></td><td>checked cm:query_faction</td></tr><tr><td><code>se.G(name)</code></td><td>read a global from the state's real global table</td></tr><tr><td><code>se.is_null(v)</code></td><td>null-interface test that also works on list interfaces</td></tr><tr><td><code>se.load_emperor_policy()</code></td><td>restore the saved emperor policy and its listener</td></tr><tr><td><code>se.load_income_lines()</code></td><td>restore saved income lines and their listener</td></tr><tr><td><code>se.log(s)</code></td><td>log through se.logger / ModLog / the DLL log</td></tr><tr><td><code>se.modify.alliance_name(cqi, text [, mode])</code></td><td>rename an alliance / coalition</td></tr><tr><td><code>se.modify.attitude(a, b, level)</code></td><td>fire an attitude-change event, level -3..3</td></tr><tr><td><code>se.modify.autoresolve_plan(plan [, ctx])</code></td><td>store a plan for the current pending battle</td></tr><tr><td><code>se.modify.autoresolve_plan_clear()</code></td><td>drop the stored plan</td></tr><tr><td><code>se.modify.autoresolver_variable(key, value)</code></td><td>retune one autoresolver_* constant (session)</td></tr><tr><td><code>se.modify.autoresolver_variables_reset()</code></td><td>all auto-resolver constants back to engine values</td></tr><tr><td><code>se.modify.build_number(build, short, modified)</code></td><td>replace the main-menu build strings</td></tr><tr><td><code>se.modify.building_construct(region, slot, level [, opts])</code></td><td>build / upgrade / convert, optionally forced and free</td></tr><tr><td><code>se.modify.building_damage(region, slot, percent)</code></td><td>damage a building</td></tr><tr><td><code>se.modify.building_destroy(region, slot)</code></td><td>destroy a building</td></tr><tr><td><code>se.modify.building_repair(region, slot [, opts])</code></td><td>repair a building (opts.free)</td></tr><tr><td><code>se.modify.cai_personality(faction, personality)</code></td><td>swap an AI faction's CAI personality</td></tr><tr><td><code>se.modify.character_add_xp(cqi, n [, scaled])</code></td><td>add exact or engine-scaled experience</td></tr><tr><td><code>se.modify.disband(cqi, slot)</code></td><td>empty a retinue slot</td></tr><tr><td><code>se.modify.effect_bundle_apply_custom(faction, bundle, effects [, turns])</code></td><td>apply a bundle with a per-instance effect list</td></tr><tr><td><code>se.modify.effect_bundle_define(bundle, effects [, faction])</code></td><td>rewrite a bundle record's effects for the session</td></tr><tr><td><code>se.modify.effect_bundle_restore(bundle [, faction])</code></td><td>restore a bundle's stock effects</td></tr><tr><td><code>se.modify.emperor_policy(policy)</code></td><td>persistent forced / banned emperor steering</td></tr><tr><td><code>se.modify.faction_income(faction, amount [, label])</code></td><td>script-side per-turn income line</td></tr><tr><td><code>se.modify.faction_potential(key, value)</code></td><td>set an AI faction's potential (-100..150)</td></tr><tr><td><code>se.modify.faction_progression(key, level)</code></td><td>raise a faction to a progression level</td></tr><tr><td><code>se.modify.force_three_kingdoms([opts])</code></td><td>fill the world-leader seats with forced / banned lists</td></tr><tr><td><code>se.modify.move_character(cqi, faction [, state])</code></td><td>move a character between factions and into pool / recruited</td></tr><tr><td><code>se.modify.pool_lock(cqi, turns)</code></td><td>lock a pool character for N rounds</td></tr><tr><td><code>se.modify.pool_unlock(cqi)</code></td><td>make a pool character available now</td></tr><tr><td><code>se.modify.recruit(cqi, unit_key [, opts])</code></td><td>recruit a unit into a retinue slot</td></tr><tr><td><code>se.modify.release_to_pool(cqi)</code></td><td>recruited character -&gt; their own faction's pool</td></tr><tr><td><code>se.modify.replace(cqi, slot, unit_key [, opts])</code></td><td>recruit into an occupied slot</td></tr><tr><td><code>se.modify.skill_points(cqi, n)</code></td><td>set the unspent skill-point counter</td></tr><tr><td><code>se.modify.unit_experience(cqi, slot, level)</code></td><td>set a unit's chevron level</td></tr><tr><td><code>se.modify.unit_strength(cqi, slot, percent)</code></td><td>set a unit's strength percent</td></tr><tr><td><code>se.modify.world_leader(key)</code></td><td>grant an emperor seat directly</td></tr><tr><td><code>se.on_model(tag, f [, cb])</code></td><td>run a function on the model thread</td></tr><tr><td><code>se.profile.start(seconds [, delay [, label]])</code></td><td>sampling profiler run; reports next to the DLL</td></tr><tr><td><code>se.profile.stop()</code></td><td>end the running profile now</td></tr><tr><td><code>se.query.ai_recruitment()</code></td><td>drain the traced AI recruitment planning passes</td></tr><tr><td><code>se.query.alliances()</code></td><td>alliances with cqi, name and members</td></tr><tr><td><code>se.query.assignment(cqi [, dump])</code></td><td>active assignment key, state, rounds, province</td></tr><tr><td><code>se.query.attitude(a, b)</code></td><td>standing of a towards b</td></tr><tr><td><code>se.query.autoresolve_plan()</code></td><td>the stored plan and its encoded form</td></tr><tr><td><code>se.query.autoresolve_prediction()</code></td><td>the engine's prediction for the pending battle</td></tr><tr><td><code>se.query.autoresolver_variable(key)</code></td><td>one autoresolver_* constant</td></tr><tr><td><code>se.query.autoresolver_variables()</code></td><td>all autoresolver_* constants</td></tr><tr><td><code>se.query.build_number()</code></td><td>current main-menu build strings</td></tr><tr><td><code>se.query.building_candidates(region, slot [, opts])</code></td><td>building levels a slot can take</td></tr><tr><td><code>se.query.cai_personality(faction)</code></td><td>current CAI personality (model thread)</td></tr><tr><td><code>se.query.character(cqi)</code></td><td>character summary incl. pool / recruited state</td></tr><tr><td><code>se.query.character_xp(cqi)</code></td><td>experience, rank, max rank, skill points</td></tr><tr><td><code>se.query.effect_bundle(bundle [, faction])</code></td><td>engine dump of a bundle record</td></tr><tr><td><code>se.query.faction(key)</code></td><td>progression level, world-leader state, seats</td></tr><tr><td><code>se.query.faction_effect_value(faction, effect_id)</code></td><td>faction-level value of an effect id</td></tr><tr><td><code>se.query.faction_force_gdp(faction_key)</code></td><td>gdp_abs total on the faction and its armies (what the horde income hook adds)</td></tr><tr><td><code>se.query.faction_income(faction)</code></td><td>script-side income lines and their total</td></tr><tr><td><code>se.query.faction_potential(key)</code></td><td>potential value, base, bonus, roll</td></tr><tr><td><code>se.query.faction_xp_gain_percent(key)</code></td><td>faction character-experience-gain percentage</td></tr><tr><td><code>se.query.horde_income_hook()</code></td><td>whether the horde income hook is installed, and its income category</td></tr><tr><td><code>se.query.pending_battle()</code></td><td>full pending-battle context incl. prediction</td></tr><tr><td><code>se.query.perf()</code></td><td>counters of the DLL's caches and file probes</td></tr><tr><td><code>se.query.pool_lock(cqi)</code></td><td>pool availability status and counter (two numbers)</td></tr><tr><td><code>se.query.recruitable(cqi, slot)</code></td><td>the slot's recruitment items with cost / turns / lock reasons</td></tr><tr><td><code>se.query.region_slots(region)</code></td><td>region slots with their buildings and health</td></tr><tr><td><code>se.query.retinue(cqi)</code></td><td>retinue slots with units, strength, experience</td></tr><tr><td><code>se.query.skill_points(cqi)</code></td><td>unspent skill points</td></tr><tr><td><code>se.query.unit(cqi, slot)</code></td><td>unit key, strength, experience of one slot</td></tr><tr><td><code>se.query.unit_quality(unit_key)</code></td><td>the campaign AI's quality rows for a unit, and the best of them</td></tr><tr><td><code>se.query.world_leaders()</code></td><td>faction keys holding an emperor seat</td></tr><tr><td><code>se.region(key)</code></td><td>checked cm:query_region</td></tr><tr><td><code>se.version()</code></td><td>DLL version string</td></tr></tbody></table></div>"
        },
        {
          "level": 3,
          "id": "values-the-module-reads-from-you-and-its-own-state",
          "title": "Values the module reads from you, and its own state",
          "text": "Values the module reads from you, and its own state",
          "html": "<div class=\"doc-table\"><table><thead><tr><th>Name</th><th>Purpose</th></tr></thead><tbody><tr><td><code>se.logger</code></td><td><code>function(string)</code> used by <code>se.log</code>; set it to <code>ModLog</code> in a console script</td></tr><tr><td><code>se.core</code></td><td>the event manager; needed by everything that installs a listener</td></tr><tr><td><code>se.EFFECT_CHARACTER_XP_GAIN</code></td><td><code>385</code> (<code>0x181</code>), the character-experience-gain effect id</td></tr><tr><td><code>se.autoresolve.handler</code></td><td>the handler <code>set_handler</code> stored</td></tr><tr><td><code>se.ai_recruit.config</code> and the tables beside it</td><td>tuning of the AI recruitment policy (§3.17)</td></tr></tbody></table></div><p>Fields starting with an underscore (<code>se._income</code>, <code>se._ar_vars</code>, <code>se._ar_plan</code>, <code>se._emperor_policy</code>, ...) and <code>se.ai_recruit.enabled</code> / <code>listening</code> / <code>policy</code> are the module's own state. They are visible, but they are not API and may change with any DLL version.</p><hr>"
        },
        {
          "level": 2,
          "id": "7-open-questions-and-source-discrepancies",
          "title": "7. Open questions and source discrepancies",
          "text": "7. Open questions and source discrepancies",
          "html": "<p>Re-checked against <code>se_api.lua</code>, the DLL sources and <code>notes/*.md</code> on <strong>2026-09-19, DLL 0.40.0</strong>. These are the places where a source disagrees with another, or where something is implemented but not confirmed by a live test. Signatures follow <code>se_api.lua</code>; behaviour follows the newest dated section of the notes.</p><p><strong>Not confirmed live yet</strong></p><ol><li><strong>The multiplayer lobby check.</strong> The whole version lock rests on the lobby comparing the build string the DLL rewrites. That has <strong>not been tested on two machines</strong> - neither the join refusal between different DLL versions nor a campaign running in lockstep with the API in use. Everything else about multiplayer follows from the lockstep rules, not from a test.</li><li><strong>Persistence</strong> of forced world-leader seats, of a renamed alliance (<code>se.modify.alliance_name</code>) and of the per-instance effect list of <code>se.modify.effect_bundle_apply_custom</code>. Verified to persist across save / restart / load: <code>cai_personality</code> and <code>faction_potential</code>.</li><li><strong><code>se.modify.emperor_policy</code></strong> installs a <code>FactionTurnStart</code> listener that has never been observed running across a turn.</li><li><strong><code>se.modify.attitude</code></strong> (the change event) is implemented but not confirmed live; the per-treaty-component evaluation bias is not implemented at all.</li><li><strong>Forced new buildings.</strong> A forced construction into an empty slot was dropped at turn processing while the province was over its construction limit. Whether a forced build under the limit completes has not been tested.</li></ol><p><strong>Implemented but deliberately incomplete</strong></p><ol><li><strong><code>plan.bias</code> and <code>plan.duels[i].fate</code></strong> are validated, clamped and stored, and neither is applied. <code>winner</code> and <code>casualties</code> are applied since 0.26.2, <code>duels</code> since 0.27 (all three verified live).</li><li><strong>Multi-force sides in an auto-resolve plan.</strong> A side whose alliance summary holds more than one army record is skipped by the result rewrite; the element size of that vector is not mapped.</li><li><strong>Unit strength and chevron writes</strong> are direct field writes. The engine's own setters were never traced, so derived state could in principle go stale. The stock getters and the retinue panel agree with what is written.</li><li><strong>No direct skill grant</strong> by skill key, and <strong>no effect id -&gt; key map</strong> beyond the single named constant <code>se.EFFECT_CHARACTER_XP_GAIN</code>.</li><li><strong>Effect bundle income.</strong> A defined <code>+50 gdp_mod_all / faction_to_region_own</code> bundle produced no change in <code>projected_net_income</code>, before or after a turn. Force-scoped GDP effects are not implemented at all (the region GDP computation was never reached) - which is what the horde income hook exists for.</li></ol><p><strong>Stale text in the sources</strong></p><ol><li><strong><code>HANDOFF.md</code> is a maintainer document and its API table lags.</strong> Where the two disagree, this document and <code>se_api.lua</code> are right. Its pre-0.31 version paragraph ends at 0.24.</li><li><strong><code>se_api.lua:1657</code> still documents <code>ui_recruit_cache_ms</code> as \"default 250\"</strong>; the DLL's default is <strong>5000</strong> (<code>perf.rs:390</code>), which is what §4b lists. The comment is stale, the code is right.</li><li><strong>Example scripts carry their own version claims</strong> in their header comments (<code>se_ar_rules.lua</code> still describes the 0.24-era auto-resolve state). Trust this document.</li></ol>"
        }
      ]
    }
  ]
};
